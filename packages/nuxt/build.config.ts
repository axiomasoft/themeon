import { defineBuildConfig } from 'unbuild'

// module-builder — unbuild-пресет (P-D25); дефолтов достаточно для этого модуля,
// файл нужен для явной точки расширения при появлении второго entry.
export default defineBuildConfig({})
