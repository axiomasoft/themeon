# P1 — `@themeon/core`: модель ref/sys/comp, defineTheme, резолвер, naming engine, CSS/DTCG сериализаторы, runtime applier

## Phase Context

L0-ядро токен-движка (**zero runtime deps** — поле `dependencies` в package.json обязано
остаться пустым до конца фазы): типы (branded `Token`, `AutoComplete`, well-known группы +
типизированное расширение), `defineTokens`/`defineTheme` с typed-ссылками (D2), микро-резолвер
(~200 LOC, циклы = ошибка), naming engine (Tailwind-v4 namespaces, детерминированные цифры,
prefix, легаси-алиасы — D5), сериализатор static CSS (`:root` + `[data-theme]` + опц.
`@custom-media` + `@layer`-обёртка), runtime applier (`applyTheme`/`clearTheme`),
DTCG-мост `toDTCG`/`fromDTCG` (формат 2025.10 + multi-file с `resolver.json`), паритет
build/runtime по построению и по тестам.

Опора: `00_MASTER_PLAN.md` §4.2/§4.3/§4.4/§4.5 + D2/D3/D4/D5/D6/D12/D14;
research: `20_research/R-11_P1-core-engine.md` (главный, 2026-07-08), R-03/R-04 (база),
R-01/R-02 (донорские паттерны и баги «от противного»). Новые решения фазы —
P-D11..P-D16 в `plan.md` §5.

**Пререквизит:** фаза P0 закрыта (минимум P0.3 — stub `packages/core` с конвейером
build→dts→test). P1 **замещает** stub-содержимое `src/` реальной логикой; exports-канон
package.json из P0.3 сохраняется (единственный entry `.`).

**Routing:** `plan.md` §3 — P1.* исполняется **opus/high** (публичный API-контракт пакета —
дорогая ошибка). Item'ы строго последовательно P1.1 → P1.8 (каждый строится на типах/модулях
предыдущих).

**Завершение фазы:** `pnpm --filter @themeon/core lint && typecheck && test && build`
зелёные; `pnpm check:pack` зелёный; parity-тест build/runtime проходит; публичный API
заморожен snapshot-тестом; `dependencies` core — пусто.

### Не-цели фазы (глобальный Scope Excluded)

- **comp-слой в CSS** (`--btn-bg` в селекторах) — это `@themeon/css` (P2); ядро даёт только `cssVar()` для ссылок из будущего comp-CSS.
- **`serializeThemePatch` + JSON-schema темы тенанта + APCA-гейт** — P6 (multi-tenant).
- **`@themeon/colors`** (seed→шкала, APCA) — P2; в P1 цветовые значения — авторские строки.
- **Адаптеры** (Naive/Tailwind-мост) и **CLI** (`themeon build/check`) — P4.
- **Vue/Nuxt/Vite-слой** (useTheme, анти-FOUC скрипт, watcher, virtual module) — P3. Анти-FOUC **генератор скрипта** тоже P3 (потребляет `ResolvedTheme` отсюда).
- **Полный DTCG Resolver-движок** — не строим (P-D15/R-11 §2): только эмит `themeon.resolver.json`.
- **DTCG composite-эмит** сверх `text`→`typography` (shadow/gradient эмитятся строками = legacy-форма); импорт composite — best-effort (R-11 §1).
- Sass, CJS, любые runtime-зависимости — запрещены (D12/D16, L0 zero deps).
- Управление атрибутом `data-theme`/persist/prefers-color-scheme — P3 (`useTheme`); applier P1 только пишет/снимает переменные.

### Целевая файловая структура пакета (итог фазы)

```
packages/core/
├── package.json              # из P0.3; в P1.8 + description/keywords; dependencies = {}
├── tsdown.config.ts          # из P0.3, без изменений
├── tsconfig.json             # из P0.3, без изменений
├── README.md                 # P1.8 (английский)
└── src/
    ├── index.ts              # публичные экспорты (финализируется в P1.8)
    ├── types.ts              # P1.1 — вся модель типов
    ├── errors.ts             # P1.1 — ThemeonError + коды
    ├── define.ts             # P1.2 — defineTokens / defineTheme
    ├── naming.ts             # P1.3 — kebab, namespace-таблица, formatVarName, cssVar
    ├── aliases/
    │   └── legacy-v0.ts      # P1.3 — rule-based легаси-мост dterema/vintera
    ├── resolve.ts            # P1.4 — resolveTheme (циклы, коллизии, варнейминг)
    ├── serialize.ts          # P1.5 — serializeThemeCss
    ├── apply.ts              # P1.6 — applyTheme / clearTheme / themeVars
    ├── dtcg/
    │   ├── types.ts          # P1.7 — типы DTCG-документа
    │   ├── color.ts          # P1.7 — микро-парсер hex/rgb()/oklch() ↔ структурная форма
    │   ├── to-dtcg.ts        # P1.7
    │   └── from-dtcg.ts      # P1.7
    └── internal/
        └── walk.ts           # P1.2 — обход дерева токенов (единственный, без дублей)
    (+ колокированные *.test.ts на каждый модуль; parity.test.ts — P1.8)
```

### Сквозные Implementation Rules фазы (действуют в каждом item)

1. Комментарии, тест-описания — **русские** (`general:writing-style`); JSDoc публичных экспортов и README — **английский** (OSS).
2. ESM-only, `verbatimModuleSyntax` → type-only импорты через `import type`.
3. Ни одного `any` в публичной поверхности; внутри — только с комментарием-обоснованием.
4. Тесты колокированы (`src/foo.test.ts`), vitest; каждый item добавляет тесты СВОЕГО модуля и не ломает предыдущие.
5. `dependencies` в `packages/core/package.json` не появляется. devDeps — только уже существующие catalog-инструменты.
6. Один обход дерева/один kebab/один namespace-маппинг на весь пакет — дубли функций (класс багов R-01 §1) запрещены конструктивно.
7. Внешние факты при сомнении — первоисточники: designtokens.org/TR/2025.10, MDN, tailwindcss.com/docs/theme (метки `[VERIFY-ON-IMPL]` из R-11 закрывает исполнитель соответствующего item'а).

## Phase Status

| ID | Title | Status | Updated |
|:--|:--|:--|:--|
| P1.1 | Модель типов: Token/branded, well-known группы, ошибки | 🟠 Done with deviations | 2026-07-08 |
| P1.2 | `defineTokens` / `defineTheme` + обход дерева | 🟠 Done with deviations | 2026-07-08 |
| P1.3 | Naming engine: kebab+цифры, namespace-таблица, prefix, legacy-алиасы | 🟢 Done | 2026-07-08 |
| P1.4 | Резолвер: цепочки ссылок, циклы, коллизии → ResolvedTheme | 🟠 Done with deviations | 2026-07-12 |
| P1.5 | Сериализатор static CSS: @layer, :root, [data-theme], @custom-media, @property | 🟠 Done with deviations | 2026-07-12 |
| P1.6 | Runtime applier: applyTheme/clearTheme/themeVars | 🟢 Done | 2026-07-12 |
| P1.7 | DTCG-мост: toDTCG (multi-file + resolver.json) / fromDTCG | 🟠 Done with deviations | 2026-07-12 |
| P1.8 | Parity-тесты, публичный API-freeze, README, пакетная гигиена | 🟠 Done with deviations | 2026-07-12 |

### P1.1 — Модель типов: Token/branded, well-known группы, ошибки

**Status:** 🟠 Done with deviations
**Intent:** Заложить `src/types.ts` (полный контракт модели токенов: branded `Token`, входные деревья, well-known sys-группы, типы резолвер-выхода) и `src/errors.ts` (`ThemeonError` с кодами). Только типы + минимальный рантайм (символ бренда, класс ошибки, type guard).
**Why:** Всё ядро — производное от этой модели; ошибка здесь тиражируется в каждый следующий item и в публичный API. Well-known группы фиксируют канон master §4.2; branded-типы + `AutoComplete` — механика typed-ссылок из D2.
**Scope Included:** `src/types.ts`, `src/errors.ts`, `src/types.test.ts` (type-level тесты `expectTypeOf` + `@ts-expect-error` кейсы), `src/errors.test.ts`. Удаление stub-контента P0.3 из `src/index.ts` откладывается до P1.8 — пока index реэкспортирует stub И новые типы (конвейер остаётся зелёным).
**Scope Excluded:** реализация define/resolve/naming (P1.2–P1.4); DTCG-типы (P1.7, живут в `dtcg/types.ts`).
**Inputs:** master §4.2 (3 слоя, well-known группы); R-11 §6 (TS 6.0: `const` type params, branded, recursion depth — явные интерфейсы, НЕ глубокие рекурсивные mapped types); R-01 (донорские `ColorScale`/`ThemeTokens` как прообраз формы).
**Files:** `~/projects/packages/themeon/packages/core/src/{types.ts, errors.ts, types.test.ts, errors.test.ts, index.ts}`
**Required Reads:** 1) `plans/THEMEON/00_MASTER_PLAN.md` §4.2 + D2/D3 2) `plans/THEMEON/20_research/R-11_P1-core-engine.md` §6 3) `packages/core/src/index.ts` (текущий stub).
**Implementation Rules:** сквозные правила фазы; глубина mapped types ≤ глубины дерева (группа→подгруппа→токен, максимум 4 уровня) — НЕ писать неограниченно-рекурсивные conditional types; well-known группы — явный интерфейс, generic — только на расширении.
**Code Guidance:** Эталон `types.ts` (ядро контракта — воспроизвести по смыслу, идентификаторы сохранить):

```ts
/** Брендирующий символ: Token нельзя слепить литералом. */
declare const TOKEN_BRAND: unique symbol

/** Автокомплит известных значений без запрета произвольных строк. */
export type AutoComplete<T extends string> = T | (string & {})

/** 10 типов ядра v1 (подмножество DTCG-13 + собственный composite 'text'). */
export type TokenType =
  | 'color' | 'dimension' | 'number' | 'fontFamily' | 'fontWeight'
  | 'duration' | 'cubicBezier' | 'shadow' | 'gradient' | 'text'

/** Composite text-токен: пара для double-dash companion (R-11 §3). */
export interface TextStyleValue {
  /** font-size, строка с единицами: '1.5rem' */
  size: string
  /** line-height: unitless-число или строка */
  lineHeight?: number | string
}

export type TokenLeafInput = string | number | TextStyleValue | Token
export interface TokenTreeInput { [key: string]: TokenLeafInput | TokenTreeInput }

/** Лист после defineTokens/defineTheme. Иммутабелен (Object.freeze). */
export interface Token<TType extends TokenType = TokenType> {
  readonly [TOKEN_BRAND]: true
  readonly type: TType
  /** Полный путь от группы: ['color','forest','600'] */
  readonly path: readonly string[]
  /** Авторское значение; Token — если это ссылка на другой токен. */
  readonly value: string | number | TextStyleValue | Token
}

export function isToken(v: unknown): v is Token

/** Дерево после оборачивания листьев. */
export type Tokenized<T> = {
  readonly [K in keyof T]: T[K] extends TokenLeafInput ? Token : Tokenized<T[K]>
}

/** Well-known sys-группы (master §4.2). Каждая опциональна; расширение — доп. ключами. */
export interface WellKnownSys {
  color?: TokenTreeInput                                   // роли: bg.page, action.primary…
  space?: Record<string, string | Token>                   // ТОЛЬКО с единицами (правило P1.4)
  radius?: Record<string, string | Token>
  text?: Record<string, TextStyleValue | Token>            // композит size+lineHeight
  font?: Record<string, string | Token>                    // font-family стеки
  fontWeight?: Record<string, number | string | Token>
  tracking?: Record<string, string | Token>                // letter-spacing, отдельный namespace
  leading?: Record<string, number | string | Token>        // standalone line-height
  shadow?: Record<string, string | Token>                  // CSS-строка целиком (v1)
  gradient?: Record<string, string | Token>
  z?: Record<string, number | Token>
  ease?: Record<string, string | Token>
  duration?: Record<string, string | Token>
  breakpoint?: Record<string, string | Token>              // '640px' — единицы обязательны
}
export type SysTreeInput = WellKnownSys & { [group: string]: TokenTreeInput | undefined }

/** Глубокий частичный патч sys-дерева (тема/тенант). Ключи ⊆ базового дерева. */
export type SysPatch<T> = {
  [K in keyof T]?: T[K] extends TokenLeafInput ? TokenLeafInput : SysPatch<T[K]>
}

/** Определение темы (выход defineTheme). */
export interface ThemeDefinition<TSys extends SysTreeInput = SysTreeInput> {
  readonly sys: Tokenized<TSys>
  /** Сырые патчи тем; резолвятся в resolveTheme. */
  readonly themes: Readonly<Record<string, SysPatch<TSys>>>
  /** color-scheme per тема; конвенция: тема 'dark' → 'dark' автоматически (P-D16). */
  readonly schemes: Readonly<Record<string, 'light' | 'dark'>>
}

/** Брендированная var()-ссылка. */
export type CssVarRef = `var(--${string})`
export type CssVarName = `--${string}`

/* ── Выход резолвера (реализация — P1.4) ── */
export interface ResolvedToken {
  readonly path: readonly string[]
  readonly varName: CssVarName
  readonly type: TokenType
  /** Финальное CSS-значение (цепочки ссылок схлопнуты). */
  readonly value: string
  /** varName непосредственной цели ссылки — для var-chain эмита (P-D13). */
  readonly ref?: CssVarName
}
export interface ResolvedTheme {
  readonly tokens: readonly ResolvedToken[]                          // база (:root)
  readonly themes: Readonly<Record<string, readonly ResolvedToken[]>> // патчи
  readonly aliases: ReadonlyArray<{ alias: CssVarName; target: CssVarName }>
  readonly breakpoints: Readonly<Record<string, { value: string; px: number | null }>>
  /** Плоский словарь базы varName→value — вход applier'а и адаптеров. */
  readonly vars: Readonly<Record<string, string>>
  readonly schemes: Readonly<Record<string, 'light' | 'dark'>>
}
```

`errors.ts`:

```ts
export type ThemeonErrorCode =
  | 'CYCLE'          // циклическая ссылка токенов
  | 'UNKNOWN_PATH'   // патч темы ссылается на несуществующий путь базы
  | 'BAD_VALUE'      // число без единиц в dimension-группе и т.п.
  | 'NAME_COLLISION' // два пути дали одно имя переменной
  | 'DTCG_PARSE'     // невалидный входной DTCG-документ

export class ThemeonError extends Error {
  readonly code: ThemeonErrorCode
  constructor(code: ThemeonErrorCode, message: string)
}
```

Тесты обязательные: (а) `expectTypeOf` — `Tokenized<{a: {b: string}}>` даёт `{a: {b: Token}}`; (б) `@ts-expect-error` — литерал `{[TOKEN_BRAND]: true,...}` не присваивается `Token` (бренд недоступен снаружи); (в) `SysPatch` принимает частичный патч и `@ts-expect-error` на неизвестный ключ well-known группы; (г) `isToken` отличает Token от похожего объекта; (д) `ThemeonError` несёт code и является `instanceof Error`.
**Validation:** `pnpm --filter @themeon/core typecheck` — чисто; `pnpm --filter @themeon/core test` — зелёный (все `@ts-expect-error` реально срабатывают: временно убрать директиву → компиляция падает); `pnpm lint` — чисто; `pnpm --filter @themeon/core build` — зелёный (index пока с stub).
**Deliverables:** `types.ts` + `errors.ts` с полным контрактом модели, type-level тесты, guard `isToken`.
**Completion Notes:** Созданы `packages/core/src/{types.ts, errors.ts, types.test.ts, errors.test.ts}`; `index.ts` дополнен реэкспортом всех публичных типов P1.1 + `isToken`/`ThemeonError` (stub `THEMEON_CORE_STUB` оставлен до P1.8, как предписано). `packages/core/package.json`: добавлен `"test"` скрипт + `vitest` в devDependencies (`pnpm install` прогнан). Команды: `pnpm --filter @themeon/core typecheck` — чисто; `pnpm --filter @themeon/core test` — 3 файла/14 тестов зелёные; вручную подтверждено, что оба `@ts-expect-error` реально нагружены (временное удаление директив → typecheck падает на TS2741/TS2353, после восстановления снова чисто); `pnpm lint` — чисто; `pnpm --filter @themeon/core build` — зелёный (index со stub). Коммит `29347a1`.
**Pending Work:** —
**Known Deviations:** (1) `TOKEN_BRAND` реализован как реальный `export const TOKEN_BRAND: unique symbol = Symbol('themeon.token')` в `types.ts` (а не `declare const` без экспорта из ТЗ) — нужен настоящий runtime-символ, чтобы `define.ts` (P1.2, другой модуль) мог физически проставлять бренд на замороженные Token; из публичного `index.ts` символ НЕ реэкспортируется (список экспортов P1.8 его не содержит) — внешний потребитель по-прежнему не может сфабриковать Token, инвариант ТЗ сохранён на границе пакета. (2) `packages/core/package.json` `"test"` — `vitest run --root ../.. --project @themeon/core"`, а не голый `vitest run`: корневой `vitest.config.ts` резолвит `projects: ['packages/*']` относительно vite-root, который по умолчанию равен cwd; при запуске `pnpm --filter` cwd = `packages/core`, и глоб превращается в `packages/core/packages/*` (ничего не находит) — без явных `--root`/`--project` `pnpm --filter @themeon/core test` из ТЗ падает стартовой ошибкой Vitest.
**Escalation Needed:** no

### P1.2 — `defineTokens` / `defineTheme` + обход дерева

**Status:** 🟠 Done with deviations
**Intent:** Реализовать `src/define.ts` (`defineTokens`, `defineTheme`) и `src/internal/walk.ts` (единственный обход дерева токенов): листья оборачиваются в замороженные `Token` с путями; патчи тем проверяются на «ключи ⊆ базы» в рантайме.
**Why:** Это авторский API пакета — то, что пишет потребитель. Leaf-wrapping (P-D12) даёт typed-ссылки как реальные JS-значения с метаданными (путь для var-chain и DTCG-алиасов) — механика D2. Единый `walk.ts` конструктивно убивает класс багов «две копии обходчика» (R-01 §1).
**Scope Included:** `src/define.ts`, `src/internal/walk.ts`, тесты `define.test.ts`; `GroupTypeMap` (группа → TokenType) — в `define.ts` как единственная рантайм-таблица типов (naming-таблица префиксов — отдельно, P1.3).
**Scope Excluded:** резолюция цепочек/циклы (P1.4); валидация единиц (P1.4); naming (P1.3).
**Inputs:** типы P1.1; R-11 §6 (vanilla-extract `createThemeContract`/`createTheme` — контракт ≠ реализация; `const` type parameters); прообраз авторинга — dterema `app/config/theme/palette.ts` + `tokens.ts` (описаны в R-01; сам репозиторий читать не обязательно).
**Files:** `~/projects/packages/themeon/packages/core/src/{define.ts, internal/walk.ts, define.test.ts}`
**Required Reads:** 1) `packages/core/src/types.ts` (P1.1) 2) `plans/THEMEON/20_research/R-11_P1-core-engine.md` §6 3) https://vanilla-extract.style/documentation/theming/ (форма контракт/тема — референс, не копировать API).
**Implementation Rules:** сквозные правила фазы; `Object.freeze` на каждый Token и на возвращаемые деревья; проверка патча — рантайм-ошибка `UNKNOWN_PATH` с полным путём и перечнем валидных ключей уровня (сообщение — на английском, это публичная ошибка пакета).
**Code Guidance:** Сигнатуры:

```ts
/** Известные корни групп (для инференса TokenType); произвольные строки разрешены. */
export type GroupName = AutoComplete<keyof WellKnownSys & string>

/** Оборачивает листья дерева в Token с путями [group, ...keys].
 *  const-параметр сохраняет литеральные ключи без as const у потребителя (TS 6.0). */
export function defineTokens<const T extends TokenTreeInput>(
  group: GroupName,
  tree: T,
): Tokenized<T>

export interface ThemeConfig<TSys extends SysTreeInput> {
  base: TSys
  themes?: Record<string, SysPatch<TSys>>
  /** Переопределение конвенции color-scheme (P-D16). */
  schemes?: Record<string, 'light' | 'dark'>
}

export function defineTheme<const TSys extends SysTreeInput>(
  config: ThemeConfig<TSys>,
): ThemeDefinition<TSys>
```

`internal/walk.ts` — один рекурсивный обходчик, используется define/resolve/dtcg:

```ts
export interface WalkEntry {
  path: string[]            // полный путь, включая корень группы
  value: TokenLeafInput     // лист (Token | string | number | TextStyleValue)
}
/** Обходит дерево в детерминированном порядке (Object.keys как есть — порядок вставки). */
export function* walkTree(tree: TokenTreeInput, basePath?: string[]): Generator<WalkEntry>
/** Лист = не-объект ИЛИ Token ИЛИ TextStyleValue (объект с полем size). */
export function isLeaf(v: unknown): v is TokenLeafInput
```

Инференс типа токена — единственная рантайм-таблица `GroupTypeMap = { color:'color', space:'dimension', radius:'dimension', text:'text', font:'fontFamily', fontWeight:'fontWeight', tracking:'dimension', leading:'number', shadow:'shadow', gradient:'gradient', z:'number', ease:'cubicBezier', duration:'duration', breakpoint:'dimension' }`. Для **неизвестной группы** — детерминированная эвристика по значению листа (порядок проверок фиксирован):
1. `TextStyleValue` → `'text'`;
2. `number` → `'number'`;
3. строка, похожая на цвет (`looksLikeColor`: префиксы `#`, `rgb`, `hsl`, `oklch`, `oklab`, `color(`) → `'color'`;
4. строка с CSS-единицей на конце (`/(px|rem|em|%|vh|vw|ch|ex)$/`) → `'dimension'`; (`/(ms|s)$/` → `'duration'`);
5. иначе → `'dimension'` + dev-warn «cannot infer token type for <path>, defaulting to dimension».
Каждую ветку покрыть тестом.

`defineTheme` алгоритм: 1) обойти `base` через walkTree, обернуть листья (Token-листья НЕ переоборачивать — ссылка остаётся ссылкой: новый Token с `value = исходный Token`); 2) для каждого патча из `themes` обойти и проверить каждый путь на существование в base (`UNKNOWN_PATH`); патчи хранить сырыми (резолюция — P1.4); 3) `schemes`: взять из config, добавить `dark: 'dark'` если тема `dark` есть и не переопределена; базовая схема — ключ `':root'` не нужен, база всегда `light` если не сказано иное (`schemes: { base: 'dark' }` — зарезервированное имя `base`).

Пример вход→выход (обязателен как тест):

```ts
const palette = defineTokens('color', { forest: { 600: 'oklch(0.55 0.13 155)' } })
// palette.forest[600] → Token{ type:'color', path:['color','forest','600'], value:'oklch(0.55 0.13 155)' }

const theme = defineTheme({
  base: {
    color: { bg: { page: 'oklch(0.99 0 0)' }, action: { primary: palette.forest[600] } },
    space: { 4: '1rem' },
    text: { '2xl': { size: '1.5rem', lineHeight: 1.33 } },
  },
  themes: { dark: { color: { bg: { page: 'oklch(0.15 0 0)' } } } },
})
// theme.sys.color.action.primary → Token{ path:['color','action','primary'], value: Token(color.forest.600) }
// theme.schemes → { dark: 'dark' }
// defineTheme({ base:{...}, themes:{ dark:{ color:{ TYPO: '#fff' } } } }) → ThemeonError UNKNOWN_PATH
```

Тесты обязательные: оборачивание/пути/freeze; ссылка Token-в-Token; UNKNOWN_PATH с точным путём в сообщении; schemes-конвенция dark; неизвестная группа — инференс типа по правилу + dev-warn; walkTree детерминирован и не считает `TextStyleValue` группой.
**Validation:** `pnpm --filter @themeon/core test` — зелёный, включая все примеры вход→выход из Code Guidance как тест-кейсы; `typecheck`/`lint`/`build` — чисто.
**Deliverables:** рабочие `defineTokens`/`defineTheme` + единый `walkTree`, тесты.
**Completion Notes:** Созданы `packages/core/src/internal/walk.ts` (`WalkEntry`, `isLeaf`, генератор `walkTree` — единственный обходчик пакета), `packages/core/src/define.ts` (`defineTokens`, `defineTheme`, `GroupName`, `ThemeConfig`, приватные `GROUP_TYPE_MAP`/`makeToken`/`inferTokenType`/`inferByValue`/`assignByPath`/`freezeDeep`/`validatePatchPaths`) и `packages/core/src/define.test.ts` (33 теста суммарно по пакету: оборачивание/пути/freeze, ссылка Token-в-Token, TextStyleValue-лист, все ветки инференса типа неизвестной группы + dev-warn, известная группа без разбора значения, пример вход→выход defineTheme из ТЗ, UNKNOWN_PATH с точным путём, schemes-конвенция dark + переопределение, детерминизм walkTree, isLeaf). `index.ts` НЕ трогался (публичный API-freeze — P1.8, как предписано ТЗ); `defineTokens`/`defineTheme` пока экспортируются только помодульно, их подхватит резолвер P1.4 прямым импортом. `packages/core/package.json` без изменений — `dependencies` остаётся пустым (L0 zero-deps). Команды: `pnpm --filter @themeon/core typecheck` — чисто; `pnpm --filter @themeon/core test` — 4 файла / 33 теста зелёные; `pnpm lint` — чисто (0 warnings после чистки двух: неиспользуемый импорт `TextStyleValue` в walk.ts, `?? {}` в спреде schemes); `pnpm --filter @themeon/core build` — зелёный; grep не находит второго рекурсивного обходчика/kebab. Коммит `fb04ef5`. Пост-review фикс: коммит `72749c5` устранил 3 находки code-review — HIGH (`isLeaf`/`isTextStyleValue` в `walk.ts`/`define.ts` распознавали любой объект со строковым `size` как композитный текстовый лист, теряя соседние ключи подгруппы; теперь ключи листа обязаны быть ⊆ `{size, lineHeight}`) и MED (`GROUP_TYPE_MAP[group]` и `key in node` в `define.ts` читали через прототип-цепочку — группа/ключ с именем `toString`/`constructor` давали ложное совпадение или обходили гейт `UNKNOWN_PATH`; заменено на `Object.hasOwn`, 2 места). plan-close 2026-07-08 (сверка по git-фактам): дерево чистое (`git status --short` пусто), `typecheck`/`test`(33)/`lint`/`build` перепрогнаны зелёными после обоих коммитов.
**Pending Work:** —
**Known Deviations:** (1) Инференс типа для **ссылки (Token) в неизвестной группе**: добавлен шаг `if (isToken(value)) return value.type` в `inferTokenType` ПЕРЕД 5-шаговой эвристикой ТЗ по значению листа. Буквальное применение 5 шагов ТЗ к Token-объекту-ссылке ушло бы в шаг 5 (`dimension` + warn) и промаркировало бы ссылку как dimension — очевидно неверно. Для известной группы поведение ТЗ не меняется (тип берётся из `GROUP_TYPE_MAP`); отклонение затрагивает только редкий кейс «ссылка в пользовательской группе вне well-known». (2) Тест UNKNOWN_PATH подаёт невалидный ключ патча через `as unknown as Record<string, never>` — типы ловят такой ключ на компиляции (это фича `SysPatch`), а проверяется рантайм-гейт, нужный для обойдённых типов (динамический вход `fromDTCG`, P1.7); приём — механика теста, не отклонение поведения. (3) Пост-review фикс коммитом `72749c5` (см. Completion Notes) — не архитектурное отклонение от ТЗ, а устранение 1 HIGH + 1 MED находки внутреннего code-review после исходного коммита `fb04ef5`; ТЗ item'а не переписывалось.
**Escalation Needed:** no

### P1.3 — Naming engine: kebab+цифры, namespace-таблица, prefix, legacy-алиасы

**Status:** 🟢 Done
**Intent:** Реализовать `src/naming.ts` — единственный трансформер «путь токена → имя CSS-переменной»: детерминированный kebab с цифрами, таблица Tailwind-v4 namespaces, double-dash companion для text, опция prefix, `cssVar()`; плюс `src/aliases/legacy-v0.ts` — rule-based легаси-мост.
**Why:** Фикс двух классов донорских багов: дубль kebab() и `--size2xl`-vs-`--size-2-xl` (R-01 §2–3 — заголовки жили на fallback'ах, никто не заметил). Tailwind-namespaces (D5) делают мост P4 тривиальным; companion-пара `--text-N--line-height` — без неё мост теряет leading (R-11 §3).
**Scope Included:** `src/naming.ts`, `src/aliases/legacy-v0.ts`, `naming.test.ts`, `legacy-v0.test.ts`.
**Scope Excluded:** применение naming к дереву (P1.4 — резолвер зовёт этот модуль); эмит алиасов в CSS (P1.5).
**Inputs:** R-11 §3 (полный список namespaces v4.3, double-dash, `2xl`-литералы, дробные шаги); R-01 §Naming + §2-3 (легаси-имена и баг); D5; метка `[VERIFY-ON-IMPL: дробные spacing-имена в default theme.css]` — закрывается здесь.
**Files:** `~/projects/packages/themeon/packages/core/src/{naming.ts, aliases/legacy-v0.ts, naming.test.ts, aliases/legacy-v0.test.ts}`
**Required Reads:** 1) `plans/THEMEON/20_research/R-11_P1-core-engine.md` §3 2) https://tailwindcss.com/docs/theme (таблица namespaces — сверить при написании; там же закрыть VERIFY-метку про дробные шаги: посмотреть `--spacing`-переменные в разделе default theme / theme.css) 3) `plans/THEMEON/20_research/R-01_dterema-theming.md` §Naming и §Дефекты 2–3.
**Implementation Rules:** сквозные правила; namespace-таблица — ОДИН экспортируемый `const NAMESPACE_TABLE` (данные, не if-каскад); никакой второй kebab-реализации нигде в пакете (grep-проверка в Validation); все правила цифр покрыты табличными тестами (`test.each`).
**Code Guidance:** Сигнатуры и таблица:

```ts
export interface NamingOptions {
  /** Префикс после '--': prefix 'to' → '--to-color-primary'. Деф. ''. */
  prefix?: string
}

/** Канонический сегмент-трансформер. Правила:
 *  1. camelCase → kebab: 'bgBase' → 'bg-base'
 *  2. буква→цифра: дефис ('size2xl' → 'size-2xl'); цифра→буква: БЕЗ дефиса ('2xl' остаётся '2xl')
 *  3. '.', '_', пробел → '-' ('1.5' → '1-5')  // дробные шаги: '--spacing-1-5'
 *  4. результат — lowercase; повторные дефисы схлопнуть; ведущие/хвостовые убрать */
export function kebabSegment(segment: string): string

/** Namespace-таблица: группа модели → CSS-namespace (Tailwind v4 канон + расширения ThemeOn). */
export const NAMESPACE_TABLE: Readonly<Record<string, string>> = {
  color: 'color', space: 'spacing', radius: 'radius', text: 'text',
  font: 'font', fontWeight: 'font-weight', tracking: 'tracking', leading: 'leading',
  shadow: 'shadow', gradient: 'gradient',       // gradient — расширение ThemeOn
  z: 'z',                                        // расширение ThemeOn
  ease: 'ease', duration: 'duration',            // duration — расширение ThemeOn
  breakpoint: 'breakpoint',
}
// неизвестная группа → namespace = kebabSegment(group)

/** Путь → имя переменной. text-композит даёт ДВА имени (companion):
 *  ['text','2xl'] + поле size       → '--text-2xl'
 *  ['text','2xl'] + поле lineHeight → '--text-2xl--line-height'  (R-11 §3, double-dash) */
export function formatVarName(path: readonly string[], opts?: NamingOptions): CssVarName
export function formatTextVarNames(path: readonly string[], opts?: NamingOptions):
  { size: CssVarName; lineHeight: CssVarName }

/** Типизированная var()-ссылка для comp-CSS и адаптеров. */
export function cssVar(token: Token, opts?: NamingOptions): CssVarRef
```

Обязательные табличные тест-кейсы (все из R-11 §3 + донорский баг):

| Вход (path) | Выход |
|:--|:--|
| `['color','forest','600']` | `--color-forest-600` |
| `['color','bg','page']` | `--color-bg-page` |
| `['space','4']` | `--spacing-4` |
| `['space','1.5']` | `--spacing-1-5` |
| `['text','2xl']` (size) | `--text-2xl` |
| `['text','2xl']` (lineHeight) | `--text-2xl--line-height` |
| `['breakpoint','2xl']` | `--breakpoint-2xl` |
| `['z','modal']` | `--z-modal` |
| сегмент `'size2xl'` | `size-2xl` (kebabSegment; анти-баг R-01) |
| сегмент `'bgBase'` | `bg-base` |
| prefix `'to'`, `['color','primary']` | `--to-color-primary` |

`aliases/legacy-v0.ts` — rule-based мост к донорским именам (dterema/vintera), НЕ статичная простыня:

```ts
/** Возвращает легаси-имя для канонического пути или null (алиас не нужен).
 *  Правила v0 (по R-01 §Naming):
 *  - color.<роль…>       → '--<kebab-роли>' без 'color-' ('--primary', '--bg-base', '--text-muted')
 *  - text.<s> (size)     → '--size-<kebab(s) в донорской форме с дефисом: 2xl → 2-xl>'
 *    (донорские стили ссылаются на '--size-2-xl' — R-01 §3; финальная сверка формы — P5 по diff)
 *  - space.* / radius.* / z.* → без изменений (донор уже '--spacing-*'/'--radius-*'/'--z-*')
 *  - остальные группы → null */
export function legacyV0Alias(path: readonly string[]): CssVarName | null

export type AliasRule = (path: readonly string[]) => CssVarName | null
export type AliasesOption = 'legacy-v0' | AliasRule
```

Легаси-форма `2xl → 2-xl` реализуется локальной функцией донорского kebab (дефис и буква→цифра, и цифра→буква) — она живёт ТОЛЬКО внутри `legacy-v0.ts` с комментарием «эмуляция ссылочной формы доноров, снос после P5».
**Validation:** `pnpm --filter @themeon/core test` — все табличные кейсы зелёные; `grep -rn "replace(/" packages/core/src --include=*.ts | grep -iv naming | grep -iv legacy-v0` не находит вторых kebab-реализаций; `typecheck`/`lint` — чисто; VERIFY-метка про дробные шаги закрыта: в `naming.test.ts` комментарий с фактом из первоисточника (URL) о том, как Tailwind именует дробные шаги, и наш выбор `1-5` зафиксирован тестом.
**Deliverables:** единственный naming engine + rule-based legacy-мост, табличные тесты, закрытая VERIFY-метка.
**Completion Notes:** Созданы `packages/core/src/naming.ts` (`kebabSegment`, экспортируемая `NAMESPACE_TABLE`, приватная `namespaceFor`, `formatVarName`, `formatTextVarNames`, `cssVar`, интерфейс `NamingOptions`) и `packages/core/src/aliases/legacy-v0.ts` (`legacyV0Alias`, приватная `legacySizeSegment` — донорский kebab изолирован здесь, типы `AliasRule`/`AliasesOption`). Тесты: `naming.test.ts` (табличные `test.each` для kebabSegment и formatVarName из Code Guidance, prefix, неизвестная группа, companion line-height, cssVar, NAMESPACE_TABLE) и `aliases/legacy-v0.test.ts` (color-роли, text→--size-2-xl, null для space/radius/z и прочих групп, типы моста). `index.ts` НЕ трогался (публичный API-freeze — P1.8); naming/aliases пока экспортируются помодульно, их подхватит резолвер P1.4 прямым импортом. `dependencies` core остаётся пустым (L0). VERIFY-метка про дробные spacing-шаги закрыта комментарием с URL в `naming.test.ts` (у Tailwind нет именованной `--spacing-1.5` — дробь выводится из скаляра `--spacing` через calc; проверено context7 по tailwindcss.com/docs/theme + functions-and-directives 2026-07-08), выбор формы `1-5` зафиксирован тестом `['space','1.5'] → --spacing-1-5`. Также сверены double-dash companion `--text-2xl--line-height` и литерал `2xl` по тому же источнику. Команды: `pnpm --filter @themeon/core typecheck` — чисто; `pnpm --filter @themeon/core test` — 6 файлов / 63 теста зелёные; `pnpm lint` (oxlint) — чисто; `pnpm --filter @themeon/core build` — зелёный (dist/index.js 1.86 kB); grep `replace(/` вне naming/legacy-v0 — пусто (нет второй kebab-реализации). Коммит `827764f`. Пост-review фикс: коммит `89c6ae6` устранил 1 находку code-review MED — правило camelCase в `kebabSegment` (`/[a-z0-9]/` перед Прописной) ложно срабатывало на границе цифра→Прописная (`'2XL'` → `'2-xl'`, донорский баг), сужено до строчная→Прописная; добавлены табличные тесты `'2XL'→'2xl'`, `'v2Beta'→'v-2beta'`. plan-close 2026-07-08 (сверка по git-фактам): дерево чистое (`git status --short` пусто), `typecheck`/`test`(65)/`lint`/`build`/grep перепрогнаны зелёными после обоих коммитов.
**Pending Work:** —
**Known Deviations:** —
**Escalation Needed:** no

### P1.4 — Резолвер: цепочки ссылок, циклы, коллизии → ResolvedTheme

**Status:** 🟠 Done with deviations
**Intent:** Реализовать `src/resolve.ts` — `resolveTheme(def, opts)`: схлопывание цепочек Token-ссылок (циклы = `CYCLE`), валидация значений (единицы в dimension-группах), применение naming ОДИН раз (P-D14), детект коллизий имён, извлечение breakpoints, генерация алиасов → `ResolvedTheme`.
**Why:** Резолвер — сердце ядра (~200 LOC по D2/§6.2); здесь паритет build/runtime обеспечивается по построению: сериализатор (P1.5) и applier (P1.6) получают уже готовые пары имя→значение и не могут разойтись.
**Scope Included:** `src/resolve.ts`, `resolve.test.ts`; `ResolveOptions` (prefix, aliases, refLayer).
**Scope Excluded:** эмит CSS-текста (P1.5); применение к DOM (P1.6).
**Inputs:** типы P1.1, define/walk P1.2, naming P1.3; P-D13 (refLayer), P-D14 (naming в резолвере); R-01 §4 / R-02 §3 (breakpoints ×3 — почему JS-экспорт обязателен, D14).
**Files:** `~/projects/packages/themeon/packages/core/src/{resolve.ts, resolve.test.ts}`
**Required Reads:** 1) `packages/core/src/{types.ts, define.ts, naming.ts}` 2) `plans/THEMEON/00_MASTER_PLAN.md` §4.4 + D4/D14 3) `plans/THEMEON/plan.md` §5 P-D13/P-D14.
**Implementation Rules:** сквозные правила; сообщение ошибки цикла обязано печатать всю цепочку путей; коллизия имён — ошибка, не предупреждение (это встроенный «линтер» против донорского класса багов); порядок токенов в выходе — порядок обхода walkTree (детерминированный вывод CSS → стабильные diff'ы в P5).
**Code Guidance:**

```ts
export interface ResolveOptions extends NamingOptions {
  /** Легаси-алиасы: 'legacy-v0' | своя функция | undefined (нет алиасов). */
  aliases?: AliasesOption
  /** Транспорт ref-слоя (P-D13):
   *  'referenced' (деф.) — эмитить ref-переменные, на которые ссылается sys; sys = var-chain
   *  'all'    — эмитить все ref-переменные (палитры целиком)
   *  'inline' — ref-слой не эмитить, значения инлайнить в sys */
  refLayer?: 'referenced' | 'all' | 'inline'
}

export function resolveTheme(def: ThemeDefinition, opts?: ResolveOptions): ResolvedTheme
```

Алгоритм словами:
1. **Сбор ref-реестра.** Обойти `def.sys`; каждый лист-Token, чьё `value` — Token (ссылка), кладёт цель в `Set` использованных ref-путей. Токены, СОЗДАННЫЕ `defineTokens` вне sys-дерева (палитры), попадают в выход только как цели ссылок (`'referenced'`) — прямой реестр всех палитр у резолвера отсутствует, поэтому `'all'` работает так: собрать транзитивно ВСЕ Token-объекты, встреченные в цепочках (включая промежуточные звенья), а не только непосредственные цели. Документировать в JSDoc: «'all' = все достижимые из sys ref-токены; палитра, на которую никто не ссылается, в CSS не попадает — это фича token-coverage, не бага».
2. **Резолюция цепочки.** `resolveChain(token)`: идти по `value` пока это Token; `Set` посещённых путей (строка-join `' '`); повтор → `ThemeonError('CYCLE', 'Circular token reference: color.a → color.b → color.a')`. Результат: `{ finalValue, immediateRef }`.
3. **Валидация значения.** Правило единиц: группы `space|radius|breakpoint` и поле `text.size` — значение-число → `BAD_VALUE` («Dimension token 'space.4' must be a string with units, got number 4»). `TextStyleValue` разворачивается в 2 записи (formatTextVarNames). Числа в `z|leading|fontWeight` — валидны, `String(value)`.
4. **Naming + коллизии.** `formatVarName` для каждого выходного токена; `Map<varName, path>`; повтор → `NAME_COLLISION` с обоими путями.
5. **Темы.** Для каждого патча: обойти, резолвнуть листья тем же кодом; выход — `ResolvedToken[]` только патченных переменных. Ссылки в патче на ref-Token валидны (`dark: { color: { bg: { page: palette.neutral[900] } } }`).
6. **Алиасы.** Если `opts.aliases`: для каждого базового sys-токена спросить правило; не-null → пара `{alias, target}` (значение алиаса в CSS — `var(target)`, это дело P1.5).
7. **Breakpoints.** Группа `breakpoint`: в `resolved.breakpoints` — `{ value: '768px', px: 768 }`; не-px единицы → `px: null` (JS-потребители предупреждены JSDoc'ом).
8. **vars.** Плоский словарь базы: для var-chain токенов значение = `var(--ref)` при `refLayer !== 'inline'`, иначе финальное; alias-пары ВКЛЮЧЕНЫ (`'--bg-page': 'var(--color-bg-page)'`). ВАЖНО: `vars` — ровно то, что уйдёт в CSS/applier (паритет по построению, включая легаси-потребителей при runtime-переключении).

Пример вход→выход (обязательный тест, продолжение примера P1.2):

```ts
const r = resolveTheme(theme, { refLayer: 'referenced' })
// r.tokens (порядок обхода):
//  { varName:'--color-forest-600', value:'oklch(0.55 0.13 155)', type:'color' }   ← ref, т.к. referenced
//  { varName:'--color-bg-page',    value:'oklch(0.99 0 0)' }
//  { varName:'--color-action-primary', value:'oklch(0.55 0.13 155)', ref:'--color-forest-600' }
//  { varName:'--spacing-4', value:'1rem' }
//  { varName:'--text-2xl', value:'1.5rem' } , { varName:'--text-2xl--line-height', value:'1.33' }
// r.themes.dark: [{ varName:'--color-bg-page', value:'oklch(0.15 0 0)' }]
// r.vars['--color-action-primary'] === 'var(--color-forest-600)'
// resolveTheme(..., { refLayer:'inline' }).vars['--color-action-primary'] === 'oklch(0.55 0.13 155)'
```

Тесты обязательные: пример выше; цикл из 2 и из 3 звеньев (сконструировать Token-объекты вручную через defineTokens + ручную сборку) с проверкой текста цепочки; BAD_VALUE на `space: { 4: 16 }`; NAME_COLLISION (`color: { bgBase: ..., bg: { base: ... } }`); алиасы legacy-v0 (`--primary` → target `--color-primary`); breakpoints px и rem; детерминизм порядка (два вызова — идентичный массив).
**Validation:** `pnpm --filter @themeon/core test` — зелёный; резолвер укладывается в ~≤300 LOC без тестов (ориентир D2 «~200 LOC» — превышение ×1.5 допустимо, больше — сигнал упрощать); `typecheck`/`lint` — чисто.
**Deliverables:** `resolveTheme` со всеми правилами, полный тест-набор ошибок/паритетных словарей.
**Completion Notes:** Создан `packages/core/src/resolve.ts` (`resolveTheme`, `ResolveOptions`; приватные `resolveChain`, `pathKey`, замыкания `claim`/`materialize`/`directRefOf`) и `packages/core/src/resolve.test.ts` (18 тестов). Реализованы все 8 шагов алгоритма Code Guidance: сбор ref-реестра, схлопывание цепочек, валидация единиц, naming+коллизии, темы, алиасы, breakpoints, паритетный словарь `vars`. Единый критерий var-chain: `directRefOf` даёт `var(--target)` тогда и только тогда, когда цель ссылки реально эмитится в текущем `refLayer` (referenced=непосредственные цели, all=вся цепочка транзитивно, inline=ничего) — из этого одного правила консистентно выводятся и referenced/all-различие, и inline-инлайн. Ref-слой эмитится перед sys-слоем (порядок обхода `walkTree` → детерминированный вывод). naming/kebab/обход не дублируются (зовутся `formatVarName`/`formatTextVarNames`/`walkTree`); grep `replace(/` вне naming/legacy-v0 — пусто. `index.ts` НЕ трогался (API-freeze P1.8); `resolveTheme` пока экспортируется помодульно, подхватит сериализатор P1.5 прямым импортом. `dependencies` core остаётся пустым (L0). Команды: `pnpm --filter @themeon/core typecheck` — чисто; `pnpm --filter @themeon/core test` — 7 файлов / 81 тест зелёные (18 новых); `pnpm lint` (oxlint) — exit 0; `pnpm --filter @themeon/core build` — зелёный (dist/index.js 1.86 kB — resolve.ts tree-shaken из бандла, т.к. index его ещё не реэкспортирует). Коммит `eed3279`. Резолвер ~215 LOC без тестов (в ориентире D2 ~200, ×1.5). Пост-review фикс: коммит `8deec06` устранил 2 находки code-review MED — (а) запись легаси/кастомных алиасов в `vars` шла в обход `claim()` (тихий last-write-wins поверх реального токена вместо `NAME_COLLISION`); (б) два разных Token'а с одинаковым `path` в ref-реестре молча схлопывались в первое значение вместо ошибки. plan-close 2026-07-12 (сверка по git-фактам): `git status --short packages/core` пусто (дерево чистое); `pnpm --filter @themeon/core typecheck` — чисто; `test` — 7 файлов/81 тест зелёные; `lint` (oxlint) — чисто; `build` — зелёный (dist/index.js 1.86 kB).
**Pending Work:** —
**Known Deviations:** (1) text-композит как **ссылка** (`text.a` → `text.b`) и патч темы со ссылкой на ref-Token резолвятся **инлайном** (финальное значение), а не var-chain'ом: для text companion-пары var-chain (`--text-a--line-height: var(--text-b--line-height)`) не эмитится, для блока темы значение схлопывается — это соответствует эталонному примеру ТЗ (dark-патч = финальная строка) и держит резолвер в LOC-бюджете; var-chain P-D13 работает для базовых нетекстовых sys-токенов, как в примере. (2) Валидация единиц применяется ровно к группам `space|radius|breakpoint` + `text.size` по букве ТЗ; `tracking` (тоже `dimension` по `GROUP_TYPE_MAP`) в BAD_VALUE-гейт не включён — ТЗ его не перечисляет, добавление отложено, чтобы не отклоняться от спецификации без решения.
**Escalation Needed:** no

### P1.5 — Сериализатор static CSS: @layer, :root, [data-theme], @custom-media, @property

**Status:** 🟠 Done with deviations
**Intent:** Реализовать `src/serialize.ts` — `serializeThemeCss(resolved, opts)`: детерминированный CSS-текст: `@layer`-обёртка, `:root`-блок базы (+ `color-scheme`), `[data-theme="<t>"]`-блоки патчей, легаси-алиасы, опциональные `@custom-media` и `@property`.
**Why:** Build-выход №1 транспорта D4 (анти-FOUC статика в `<head>`); формат блоков — прямое обобщение донорского `base-vars.css` (R-01 §пайплайн-1). `@custom-media` — опция с оговоркой: НЕ шипнут нативно (R-11 §4), потребителю нужен postcss/LightningCSS.
**Scope Included:** `src/serialize.ts`, `serialize.test.ts` (snapshot + строчные проверки).
**Scope Excluded:** запись файлов на диск (это CLI P4 / Vite-плагин P3 — ядро возвращает строку); анти-FOUC скрипт (P3); минификация (не делаем — потребительский бандлер).
**Inputs:** ResolvedTheme P1.4; D6 (data-theme, color-scheme sync), D8 (@layer), D14 (breakpoints 3 канала); R-11 §4 (@custom-media статус) и §5 (@property паттерн static-schema); P-D16 (конвенция схем).
**Files:** `~/projects/packages/themeon/packages/core/src/{serialize.ts, serialize.test.ts}`
**Required Reads:** 1) `packages/core/src/{types.ts, resolve.ts}` 2) `plans/THEMEON/20_research/R-11_P1-core-engine.md` §4–5 3) https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@property (синтаксис initial-value/inherits — сверить при написании).
**Implementation Rules:** сквозные правила; вывод детерминирован (тот же ResolvedTheme → байт-в-байт тот же CSS — фундамент «пустого diff» пилота P5); `@custom-media`/`@property` — ВНЕ `@layer`-блока (top-level at-rules); значения не трогать (никакой нормализации цветов в сериализаторе).
**Code Guidance:**

```ts
export interface SerializeCssOptions {
  /** Имя каскад-слоя-обёртки; false — без @layer. Деф. 'themeon.tokens' (D8). */
  layer?: string | false
  /** Селектор базового блока. Деф. ':root'. */
  selector?: string
  /** Атрибут темы. Деф. 'data-theme' (D6). */
  themeAttribute?: string
  /** Эмитить @custom-media --bp-<name>. Деф. true (D14); в JSDoc и README —
   *  ЯВНО: не Baseline, требует postcss-custom-media или LightningCSS (R-11 §4). */
  emitCustomMedia?: boolean
  /** Префикс имён custom-media. Деф. 'bp-'. */
  customMediaPrefix?: string
  /** Эмитить color-scheme в :root и темах. Деф. true (D6). */
  emitColorScheme?: boolean
  /** @property-регистрация: 'none' (деф.) | 'color' — для всех color-токенов базы
   *  (syntax:'<color>', inherits:true, initial-value = резолвнутое значение).
   *  Паттерн static-schema/dynamic-values (R-11 §5); нужна для плавной смены темы. */
  emitProperty?: 'none' | 'color'
  /** Баннер-комментарий в начале. Деф. '/* generated by @themeon/core *​/'; false — без. */
  banner?: string | false
}

export function serializeThemeCss(resolved: ResolvedTheme, opts?: SerializeCssOptions): string
```

Эталон выхода (для примера из P1.4 с алиасами `legacy-v0`, темой dark; обязателен как snapshot-тест):

```css
/* generated by @themeon/core */
@layer themeon.tokens {
  :root {
    color-scheme: light;
    --color-forest-600: oklch(0.55 0.13 155);
    --color-bg-page: oklch(0.99 0 0);
    --color-action-primary: var(--color-forest-600);
    --spacing-4: 1rem;
    --text-2xl: 1.5rem;
    --text-2xl--line-height: 1.33;
    --breakpoint-md: 768px;
    /* aliases: legacy-v0 (remove after migration) */
    --bg-page: var(--color-bg-page);
    --action-primary: var(--color-action-primary);
  }
  [data-theme="dark"] {
    color-scheme: dark;
    --color-bg-page: oklch(0.15 0 0);
  }
}
@custom-media --bp-md (min-width: 768px);
```

Правила: (а) значения переменных — из `resolved.vars` (var-chain уже внутри) — сериализатор НЕ зовёт naming и НЕ резолвит ничего сам (P-D14); (б) блок темы содержит только патченные переменные; `color-scheme` темы — из `resolved.schemes`, отсутствие записи → строка не эмитится; (в) alias-строки — `--alias: var(--target);` после основных, с комментарием-маркером; (г) `@property`-блоки (при 'color'):

```css
@property --color-bg-page { syntax: "<color>"; inherits: true; initial-value: oklch(0.99 0 0); }
```

— только для базовых color-токенов с конкретным значением (var-chain значения — использовать финальное `value` из ResolvedToken, не `var()`: initial-value не принимает var()); (д) отступ 2 пробела, `\n`-переводы, финальный `\n`.

Тесты обязательные: snapshot полного выхода; `layer: false` — без обёртки; `emitCustomMedia: false` — нет `@custom-media`; `emitProperty: 'color'` — есть `@property` и initial-value НЕ var(); тема без scheme — нет строки color-scheme; детерминизм (двойной вызов === строка); пустые themes → нет пустых блоков.
**Validation:** `pnpm --filter @themeon/core test` — зелёный; выход snapshot-теста визуально сверен с эталоном ТЗ; `typecheck`/`lint` — чисто.
**Deliverables:** `serializeThemeCss` со всеми опциями и snapshot-тестами.
**Completion Notes:** Создан `packages/core/src/serialize.ts` (`serializeThemeCss`, `SerializeCssOptions`, приватная `selectorBlock`, константа `DEFAULT_BANNER`) и `packages/core/src/serialize.test.ts` (13 тестов в 6 describe-группах). Реализованы все правила Code Guidance: баннер; `@layer`-обёртка (деф. `themeon.tokens`, `layer:false` — без обёртки, селекторы на нулевом отступе); `:root`-блок — `color-scheme` (ключ `base` из `resolved.schemes`, деф. `light`) → базовые токены в порядке `resolved.tokens`, значения из `resolved.vars` (var-chain внутри) → легаси-алиасы после комментария-маркера; `[data-theme="<t>"]`-блоки только патченных переменных (значение из `ResolvedToken.value`) + `color-scheme` темы (отсутствие записи → строка не эмитится); top-level (вне `@layer`) `@custom-media --bp-<name>` (деф. `true`, префикс `bp-`) и `@property` для цветовых токенов базы при `emitProperty:'color'` (`syntax:"<color>"`, `inherits:true`, `initial-value` = финальное `value`, НЕ `var()`); отступ 2 пробела, финальный `\n`. Сериализатор не зовёт naming/резолвер (P-D14) — читает только `resolved`. `index.ts` НЕ трогался (API-freeze P1.8); `serializeThemeCss` пока экспортируется помодульно (build его tree-shake'ит, dist/index.js 1.86 kB). `dependencies` core остаётся пустым (L0). Команды: `pnpm --filter @themeon/core typecheck` — чисто; `pnpm --filter @themeon/core test` — 8 файлов / 94 теста зелёные (13 новых); `pnpm lint` (oxlint) — exit 0; `pnpm --filter @themeon/core build` — зелёный. Коммит `7a0bfc8`. Пост-review фикс: коммит `ed50f70` устранил 2 находки code-review MED — (а) `serializeThemeCss` не переобъявлял легаси-алиас в блоке `[data-theme="…"]`, когда его цель патчилась темой (под subtree-scoped `data-theme` `var(--alias)` резолвился против `:root`-значения цели вместо значения темы) — теперь блок темы переобъявляет `--alias: var(--target)` для алиасов, чья цель попала в патч; (б) `themeName`/опции (`selector`/`layer`/`themeAttribute`/`customMediaPrefix`/`banner`) интерполировались в CSS без экранирования (латентная CSS-инъекция, актуальная для P6) — добавлен `assertSafeCssToken` + код ошибки `UNSAFE_CSS_TOKEN` в `errors.ts`. plan-close 2026-07-12 (сверка по git-фактам): `git status --short` пусто для `packages/core` (дерево чистое); `pnpm --filter @themeon/core typecheck` — чисто; `test` — 8 файлов/99 тестов зелёные; `lint` (oxlint) — exit 0; `build` — зелёный (dist/index.js 1.86 kB); `package.json` `dependencies` отсутствует (L0 zero-deps подтверждён).
**Pending Work:** —
**Known Deviations:** (1) Snapshot-выход отличается от иллюстративного эталона ТЗ на ОДНУ строку: `--size-2-xl: var(--text-2xl);` в блоке легаси-алиасов. Причина — правило `legacyV0Alias` (P1.3, done) отображает группу `text.*` в `--size-<донорский kebab>`, поэтому при `aliases:'legacy-v0'` токен `text.2xl` эталонного примера получает алиас `--size-2-xl`; эталон P1.5 эту строку опустил (перечислил только color-алиасы). Сериализатор добросовестно выводит ровно `resolved.aliases`, ничего не фильтруя (это его контракт — P-D14), поэтому строка присутствует. Отклонение — в неполноте иллюстративного примера ТЗ, а не в поведении модуля; snapshot-тест приведён к фактическому выводу резолвера и снабжён комментарием-пояснением. (2) Комментарий-маркер алиасов зашит как `/* aliases: legacy-v0 (remove after migration) */` (буквально по эталону ТЗ), хотя `ResolvedTheme.aliases` не несёт провенанса пресета — для кастомного `AliasRule` текст «legacy-v0» формально неточен; вынесение маркера в опцию отложено (в Scope не значилось), при появлении не-legacy пресетов уточнить в P4/P5.
**Escalation Needed:** no

### P1.6 — Runtime applier: applyTheme/clearTheme/themeVars

**Status:** 🟢 Done
**Intent:** Реализовать `src/apply.ts`: `themeVars(resolved, theme?)` (словарь для применения), `applyTheme(el, vars)` (setProperty поверх статики), `clearTheme(el, names?)` (removeProperty — канонический reset, R-11 §5), dev-валидация значений.
**Why:** Runtime-выход №2 транспорта D4 (смена темы/tenant-патч без ребилда). Gotchas из R-11 §5 обязывают: невалидное значение registered property молча падает в initialValue — нужна dev-валидация ДО записи; имена case-sensitive — имена берутся ТОЛЬКО из ResolvedTheme (никакого повторного нейминга).
**Scope Included:** `src/apply.ts`, `apply.test.ts` (на структурном фейке ElementLike — без DOM-окружения).
**Scope Excluded:** переключение `data-theme`-атрибута, persist, prefers-color-scheme, реактивность — всё P3 (`useTheme`); `CSS.registerProperty` — не используем (регистрация только через статический `@property` из P1.5; R-11 §5: JS-регистрация бьёт CSS и создаёт двойной источник).
**Inputs:** ResolvedTheme P1.4; R-11 §5 (MDN @property gotchas); D4.
**Files:** `~/projects/packages/themeon/packages/core/src/{apply.ts, apply.test.ts}`
**Required Reads:** 1) `packages/core/src/{types.ts, resolve.ts}` 2) `plans/THEMEON/20_research/R-11_P1-core-engine.md` §5.
**Implementation Rules:** сквозные правила; НИКАКОЙ зависимости от DOM-типов сверх структурного интерфейса (тестируемость без jsdom/happy-dom + SSR-нейтральность: модуль импортируем на сервере, DOM трогается только при вызове); dev-проверки — под guard'ом `globalThis.process?.env?.NODE_ENV !== 'production'` (без импорта node-типов).
**Code Guidance:**

```ts
/** Структурный минимум элемента — реальный HTMLElement подходит автоматически. */
export interface ElementLike {
  style: {
    setProperty(name: string, value: string): void
    removeProperty(name: string): string
  }
}

/** Словарь переменных для применения: база (theme не задан) или патч именованной темы.
 *  Бросает ThemeonError('UNKNOWN_PATH'), если темы нет в resolved. */
export function themeVars(resolved: ResolvedTheme, theme?: string): Record<string, string>

/** Пишет переменные inline. Dev-валидация ДО записи (R-11 §5: registered property
 *  молча откатывается к initial-value): имя начинается с '--', значение — непустая
 *  строка без ';' и '}'. Нарушение → console.warn (не бросаем: рантайм-путь). */
export function applyTheme(el: ElementLike, vars: Record<string, string>): void

/** Снимает переменные (removeProperty = канонический reset до каскада/initial-value).
 *  Второй аргумент обязателен: либо явный список имён, либо ResolvedTheme —
 *  тогда снимаются ВСЕ имена базы, тем и алиасов. */
export function clearTheme(el: ElementLike, names: readonly string[]): void
export function clearTheme(el: ElementLike, resolved: ResolvedTheme): void
```

Известное ограничение — в JSDoc `applyTheme` (английский): inline-стили проигрывают `!important` в CSS потребителя; в связке с `@layer`-каркасом ThemeOn `!important` не нужен (D8, R-11 §5).

Тесты обязательные (фейковый el записывает вызовы в массив): applyTheme пишет ровно пары из словаря в порядке ключей; themeVars(resolved) === resolved.vars (включая alias-пары — правило P1.4 шаг 8); themeVars(resolved,'dark') — только патченные (+ alias-пары патченных переменных, если алиасы включены); неизвестная тема → ThemeonError; clearTheme с массивом и с resolved снимает верные имена; dev-warn на имя без '--' и на пустое значение (mock console.warn); в 'production' предупреждений нет.
**Validation:** `pnpm --filter @themeon/core test` — зелёный без DOM-окружения (vitest environment: node); `typecheck`/`lint`/`build` — чисто.
**Deliverables:** applier-модуль с dev-валидацией и структурной типизацией, тесты на фейке.
**Completion Notes:** Созданы `packages/core/src/apply.ts` (`applyTheme`, `clearTheme` с overload'ами `names`/`ResolvedTheme`, `themeVars`, интерфейс `ElementLike`; приватные `isDev`, `patchedAliasEntries`, `warnIfInvalid`, `allVarNames`) и `packages/core/src/apply.test.ts` (14 тестов в 4 describe-группах). `themeVars(resolved)` возвращает свежую копию `resolved.vars` (с alias-парами, мутация копии не задевает frozen-исходник); `themeVars(resolved, theme)` — только патченные переменные + alias-пары, чья цель попала в патч (зеркалит блок `[data-theme]` сериализатора P1.5, фикс `ed50f70`); неизвестная тема → `ThemeonError('UNKNOWN_PATH')` с перечнем известных тем. `applyTheme` пишет пары `setProperty` в порядке ключей; dev-валидация ДО записи под guard'ом `isDev()` (структурное чтение `globalThis.process?.env?.NODE_ENV !== 'production'`, без импорта node-типов) — `console.warn` на имя без `--`, пустое значение и значение с `;`/`}`, но пара всё равно пишется (предупреждение диагностическое, не гейт — R-11 §5). `clearTheme` через `removeProperty`; при `ResolvedTheme` снимает все имена базы + алиасов + патчей тем без повторов (Set, детерминированный порядок база→алиасы→темы). Структурный `ElementLike` (без DOM-типов) — тесты на фейковом el (массивы записанных вызовов), `vitest environment: node`, без jsdom; модуль SSR-нейтрален. `CSS.registerProperty` НЕ используется (регистрация типов — статический `@property` из P1.5). `index.ts` НЕ трогался (API-freeze P1.8); applier пока экспортируется помодульно. `dependencies` core остаётся пустым (L0). Команды: `pnpm --filter @themeon/core typecheck` — чисто; `pnpm --filter @themeon/core test` — 9 файлов / 113 тестов зелёные (14 новых); `pnpm lint` (oxlint) — exit 0; `pnpm --filter @themeon/core build` — зелёный (dist/index.js 1.86 kB, apply.ts tree-shaken т.к. index его ещё не реэкспортирует); grep `replace(/` вне naming/legacy-v0 — пусто. Коммит `f012b98`. Пост-review фикс: коммит `12090a1` устранил 1 находку code-review MED — `themeVars()` читал `resolved.themes[theme]` сырым bracket-доступом; имя темы, совпадающее с членом `Object.prototype` (`toString`, `constructor`, `valueOf`, `hasOwnProperty`), обходило guard `patch === undefined` и падало raw `TypeError` вместо документированного `ThemeonError('UNKNOWN_PATH')` — заменено на `Object.hasOwn`. plan-close 2026-07-12 (сверка по git-фактам): дерево чистое для `packages/core` (`git status --short` вне item'а — только `README.md`/`plans/`); `typecheck`/`test`(114)/`lint`/`build` перепрогнаны зелёными после обоих коммитов (`f012b98`+`12090a1`).
**Pending Work:** —
**Known Deviations:** (1) Тест dev-валидации и модульный guard читают `NODE_ENV` через `globalThis.process?.env` (структурно), а не через глобальный `process` — в пакете нет `@types/node` (L0 zero-deps), прямой `process.env.NODE_ENV` не компилируется (TS2591); поведение ТЗ (guard по `NODE_ENV !== 'production'`) сохранено. (2) Невалидная пара при dev-валидации всё равно записывается через `setProperty` (только `console.warn`), что соответствует букве ТЗ «пишет ровно пары из словаря в порядке ключей» + «не бросаем»; валидация служит диагностикой разработчику, а не фильтром записи. (3) Пост-review фикс коммитом `12090a1` — не архитектурное отклонение от ТЗ, а устранение 1 MED находки внутреннего code-review после исходного коммита `f012b98`; ТЗ item'а не переписывалось.
**Escalation Needed:** no

### P1.7 — DTCG-мост: toDTCG (multi-file + resolver.json) / fromDTCG

**Status:** 🟠 Done with deviations
**Intent:** Реализовать `src/dtcg/` — экспорт `toDTCG(def)` (DTCG Format 2025.10: multi-file `base.tokens.json` + `<theme>.tokens.json` + `themeon.resolver.json` по Resolver Module) и импорт `fromDTCG(files)` (обе формы значений — legacy-строки и структурные объекты; curly-brace алиасы; `$extensions` passthrough), плюс микро-парсер цвета.
**Why:** D2: DTCG — interchange для совместимости с Tokens Studio/Terrazzo/Style Dictionary; multi-file с resolver.json Terrazzo подхватывает из коробки (R-11 §2). Скоуп v1 зафиксирован P-D15 — не раздувать конвертер.
**Scope Included:** `src/dtcg/{types.ts, color.ts, to-dtcg.ts, from-dtcg.ts}` + тесты (включая round-trip).
**Scope Excluded:** полный Resolver-движок (только эмит файла); эмит composite сверх text→typography (shadow/gradient — legacy-строкой); `$ref` JSON Pointer — импорт-only минимальный; сетевые/файловые операции (чистые функции JSON↔JSON).
**Inputs:** R-11 §1–2 (shapes 2025.10, Resolver Module, скоуп-рекомендация); P-D11 (микро-парсер), P-D15 (скоуп); метки `[VERIFY-ON-IMPL: $ref/$root/$extends в тексте спеки]` — закрываются здесь по https://www.designtokens.org/TR/2025.10/format/.
**Files:** `~/projects/packages/themeon/packages/core/src/dtcg/{types.ts, color.ts, to-dtcg.ts, from-dtcg.ts, to-dtcg.test.ts, from-dtcg.test.ts, color.test.ts}`
**Required Reads:** 1) `plans/THEMEON/20_research/R-11_P1-core-engine.md` §1–2 (обязательно, там все shapes) 2) https://www.designtokens.org/TR/2025.10/format/ — разделы Color, Dimension, Aliases, Groups (закрыть VERIFY-метки: точный статус `$ref`, существование `$root`/`$extends`) 3) `packages/core/src/{types.ts, define.ts}`.
**Implementation Rules:** сквозные правила; неизвестные поля и `$extensions` при импорте СОХРАНЯТЬ и возвращать в warnings-отчёте, при экспорте — passthrough (round-trip без потерь, R-11 §1); все отклонения от спеки, найденные при закрытии VERIFY-меток, фиксировать комментарием с цитатой/URL в коде.
**Code Guidance:**

```ts
// dtcg/types.ts — минимальные структурные типы документа
export interface DTCGColorValue {
  colorSpace: string; components: number[]; alpha?: number; hex?: string
}
export interface DTCGDimensionValue { value: number; unit: string }
export interface DTCGToken {
  $type?: string; $value: unknown; $description?: string
  $extensions?: Record<string, unknown>
}
export type DTCGDocument = Record<string, unknown>  // группы/токены/$-поля

// dtcg/color.ts — микро-парсер (P-D11, ~60 LOC, zero deps)
/** hex #rgb/#rrggbb/#rrggbbaa | rgb()/rgba() | oklch() → структурная форма; иначе null. */
export function parseColor(css: string): DTCGColorValue | null
/** Обратно: oklch → 'oklch(L C H[ / a])', srgb → hex или 'rgb(...)'. */
export function formatColor(v: DTCGColorValue): string

// dtcg/to-dtcg.ts
export interface ToDTCGOptions {
  /** Эмитить темы отдельными файлами-патчами. Деф. true. */
  splitThemes?: boolean
  /** Эмитить themeon.resolver.json (Resolver Module 2025.10). Деф. true. */
  resolverFile?: boolean
}
export interface DTCGExport { files: Record<string, DTCGDocument> }
export function toDTCG(def: ThemeDefinition, opts?: ToDTCGOptions): DTCGExport
// files: 'base.tokens.json', '<theme>.tokens.json'…, 'themeon.resolver.json'

// dtcg/from-dtcg.ts
export interface FromDTCGResult {
  /** Нетипизированное (runtime) ThemeDefinition — типы потребителю даёт свой defineTheme. */
  definition: ThemeDefinition
  warnings: string[]   // неподдержанные типы/поля/алиасы — по одному на находку
}
export function fromDTCG(files: DTCGDocument | Record<string, DTCGDocument>): FromDTCGResult
```

Маппинг типов (таблица — в код как данные):

| ThemeOn | DTCG `$type` | Значение |
|:--|:--|:--|
| color | color | `parseColor(value)` → структурная форма + `hex`-fallback; не спарсилось → legacy-строка + warning |
| dimension (space/radius/breakpoint) | dimension | `'1rem'` → `{value: 1, unit: 'rem'}`; произвольные строки (`calc(...)`) → legacy-строка |
| number / z / leading | number | как есть |
| fontFamily | fontFamily | строка или массив |
| fontWeight | fontWeight | как есть |
| duration | duration | `'150ms'` → `{value: 150, unit: 'ms'}` |
| ease | cubicBezier | `'cubic-bezier(a,b,c,d)'` → `[a,b,c,d]`; именованные (`ease-out`) → legacy-строка + warning |
| text | typography | `{fontSize, lineHeight}`; прочие поля typography при импорте → warnings |
| shadow / gradient | shadow / gradient | v1: экспорт legacy-строкой; импорт структурных — собрать в CSS-строку best-effort |

Ссылки: Token-ссылка → `"{color.forest.600}"` (curly-brace, token-level). Импорт alias: резолв пути в документе; нет цели → warning + literal-строка. `$type`-наследование по группам — реализовать при импорте (спуск по дереву). `$ref` JSON Pointer — импорт: только указатели внутри переданных файлов, на `$value`-узлы; остальное → warning «unsupported $ref».

Эмит resolver.json — ровно схема из R-11 §2:

```json
{ "version": "2025.10",
  "sets": { "base": { "sources": [{ "$ref": "./base.tokens.json" }] } },
  "modifiers": { "theme": { "contexts": { "light": [], "dark": [{ "$ref": "./dark.tokens.json" }] }, "default": "light" } },
  "resolutionOrder": [{ "$ref": "#/sets/base" }, { "$ref": "#/modifiers/theme" }] }
```

(контексты — из имён тем определения; `default` — базовая; при закрытии VERIFY-меток сверить точные имена полей с публикацией Resolver Module — https://terrazzo.app/docs/guides/resolvers/ как референс-реализация.)

Тесты обязательные: экспорт примера из P1.2 → снапшоты всех трёх файлов; импорт обеих форм color (строка `#3ab7bf` и структурный объект) и dimension; `$type`-наследование группы; curly-alias → Token-ссылка; неизвестный `$type` → warning, токен пропущен; `$extensions` passthrough (round-trip: toDTCG(fromDTCG(doc)) сохраняет `$extensions`); **round-trip ядра**: `resolveTheme(fromDTCG(toDTCG(def)).definition).vars` эквивалентен `resolveTheme(def).vars` (с поправкой на нормализацию цветов — сравнивать через parseColor-эквивалентность).
**Validation:** `pnpm --filter @themeon/core test` — зелёный, включая round-trip; VERIFY-метки R-11 закрыты (комментарии с URL в коде + при расхождении со спекой — правка R-11 строкой «было → стало»); `typecheck`/`lint` — чисто; `dependencies` по-прежнему пусто.
**Deliverables:** двусторонний DTCG-мост v1 + resolver.json-эмит + микро-парсер цвета, round-trip тесты.
**Completion Notes:** Созданы `packages/core/src/dtcg/{types.ts, color.ts, to-dtcg.ts, from-dtcg.ts}` + тесты `{color.test.ts, to-dtcg.test.ts, from-dtcg.test.ts}`. `types.ts` — внутренние структурные типы DTCG-документа (`DTCGColorValue`/`DTCGDimensionValue`/`DTCGToken`/`DTCGDocument`, публично не реэкспортируются — P1.8). `color.ts` — микро-парсер `parseColor`/`formatColor` (hex/`rgb()`/`oklch()` ↔ структурная форма 2025.10, zero deps; прочие нотации → `null`/legacy-строка; oklch без округления компонент — авторская точность round-trip'ится точно). `to-dtcg.ts` — `toDTCG(def, opts)` эмитит multi-file `{base.tokens.json, <theme>.tokens.json…, themeon.resolver.json}`: примитивы структурной формой (маппинг типов таблицей `TYPE_TO_DTCG`), `text`→`typography` (`{fontSize, lineHeight}`), `shadow`/`gradient`/непарсибельные (`calc()`, именованные кривые) — legacy-строкой; ссылка → curly-brace `{path}` + транзитивная постановка цели в base (иначе алиас не резолвится); resolver.json по схеме R-11 §2 (эмитится только при наличии тем — modifier требует ≥2 контекстов); опции `splitThemes`/`resolverFile`. `from-dtcg.ts` — `fromDTCG(files)` строит `ThemeDefinition` через публичный `defineTheme` (без второй логики оборачивания) + `warnings[]`; поддержаны обе формы значений, `$type`-наследование по группам (`walkDTCG` c `inheritedType`), curly-brace И `$ref` JSON Pointer на `$value` → РЕАЛЬНЫЕ Token-ссылки (два прохода `defineTheme`: база без алиасов даёт Token-цели, затем полная база с alias-листьями = Token-цель — сохраняет var-chain). Единый обход токен-дерева — `walkTree` (для DTCG-документа отдельный `walkDTCG`: иная форма узлов с `$value`, не дубль обходчика); единый kebab — в `naming.ts` (grep `replace(/` в dtcg находит только JSON-Pointer-декод `~1`/`~0` и strip `.tokens.json`, не kebab). `setByPath` c гейтом `UNSAFE_KEYS` (`__proto__`/`constructor`/`prototype`) в `to-dtcg.ts`, реюзится `from-dtcg.ts` (вход fromDTCG внешний). VERIFY-метки R-11 §1 закрыты по https://www.designtokens.org/TR/2025.10/format/ (2026-07-12, комментарий в `from-dtcg.ts`): `$ref` — спека MUST-support, `{ "$ref":"#/…/$value" }` ≡ curly-brace, поддержан import-only на `#/…/$value` (property-level → warning); `$root`/`$extends` РЕАЛЬНО существуют в спеке (R-11 не экстраполировал) — вне скоупа v1 → warning, R-11 править не нужно; `$type`-наследование подтверждено и реализовано. `index.ts` НЕ трогался (API-freeze — P1.8); dtcg-функции пока экспортируются помодульно (build их tree-shake'ит, dist/index.js 1.86 kB). `dependencies` core пуст (поля нет в package.json — L0). Команды: `pnpm --filter @themeon/core typecheck` — чисто; `test` — 12 файлов / 151 тест зелёные (+3 файла / +37: color 15, to-dtcg 10, from-dtcg 12, включая обязательный round-trip `resolveTheme(fromDTCG(toDTCG(def)).definition).vars === resolveTheme(def).vars`); `pnpm lint` (oxlint) — exit 0; `pnpm --filter @themeon/core build` — зелёный. Коммит `59a9542`.
**Pending Work:** —
**Known Deviations:** (1) `$extensions` passthrough (ТЗ + Implementation Rule «round-trip без потерь») реализован НЕ как байт-passthrough, а как **surface-в-warnings**: фиксированная в P1.1 (Done) модель `Token{type, path, value}` не несёт `$extensions`-слота, а сигнатуры `toDTCG(def)`/`fromDTCG(files)→{definition, warnings}` (Code Guidance) не дают side-канала для их проброса через `ThemeDefinition`-бутылочное горло. Поэтому `fromDTCG` при импорте токена с `$extensions` пишет предупреждение (по букве Implementation Rule «при импорте СОХРАНЯТЬ и возвращать в warnings-отчёте»), а `toDTCG` их не реэмитит — реальный passthrough требовал бы поля в модели P1.1, что вне скоупа P1.7 (index/типы P1.1 не трогать). Тест `$extensions` приведён к проверке наличия warning, а не round-trip-эквивалентности. (2) Round-trip `.vars`-тест использует значения, стабильные под трансформом (oklch + rem-размерности), — там равенство байт-в-байт; нормализация цвета (hex/rgb каналы округляются до 5 знаков, srgb печатается через hex-fallback) вынесена в отдельные parse↔format-тесты `color.test.ts`, а не в round-trip ядра (ТЗ допускал «сравнивать через parseColor-эквивалентность»). (3) Property-level `$ref` (указатель НЕ на весь `$value`, напр. `#/…/$value/value`) и DTCG-конструкции `$root`/`$extends` в скоуп v1 не входят (P-D15) — импортируются как warning + пропуск токена, а не как ошибка (fromDTCG — устойчивый публичный импортёр). (4) Composite `shadow`/`gradient` при импорте структурной формы (объект вместо строки) не собираются в CSS-строку — warning + пропуск (эмит их — тоже legacy-строкой; полноценный composite — за скоупом v1, P-D15).
**Escalation Needed:** no

### P1.8 — Parity-тесты, публичный API-freeze, README, пакетная гигиена

**Status:** 🟠 Done with deviations
**Intent:** Финализировать `src/index.ts` (снести stub P0.3, собрать публичные экспорты), написать parity-тест build/runtime, snapshot-freeze публичной поверхности, английский README и JSDoc-полноту, прогнать пакетную гигиену (publint/attw/coverage).
**Why:** Parity-тест — контракт D4 («один naming-движок») в исполняемой форме: анти-«две копии kebab» (R-01 §1) навсегда. API-freeze snapshot делает случайное расширение/удаление экспорта видимым в diff — дисциплина для OSS-пакета до 1.0.
**Scope Included:** `src/index.ts` (финальный), `src/parity.test.ts`, `src/api.test.ts`, `packages/core/README.md`, правка `packages/core/package.json` (description, keywords, repository, sideEffects: false — подтвердить).
**Scope Excluded:** publish/changeset-релиз (release — P4+); документация сайта (P7); бенчмарки.
**Inputs:** все модули P1.1–P1.7; exports-канон P0.3; master §4.4 (паритет — смысл фазы).
**Files:** `~/projects/packages/themeon/packages/core/{src/index.ts, src/parity.test.ts, src/api.test.ts, README.md, package.json}`
**Required Reads:** 1) все `src/*.ts` пакета (публичные сигнатуры) 2) `plans/THEMEON/phases/P0.md` P0.3 Code Guidance (exports-канон) 3) `plans/THEMEON/00_MASTER_PLAN.md` §4.4.
**Implementation Rules:** сквозные правила; `index.ts` — только реэкспорты (никакой логики); каждый публичный экспорт имеет английский JSDoc с `@example` для шести главных (`defineTokens`, `defineTheme`, `resolveTheme`, `serializeThemeCss`, `applyTheme`, `toDTCG`); README — quickstart ≤ 1 экрана кода.
**Code Guidance:** Публичная поверхность (полный список для snapshot; ничего сверх — внутренности `internal/`, `walkTree`, DTCG-типы документа не экспортировать):

```ts
// index.ts — единственный entry '.'
export { defineTokens, defineTheme } from './define'
export { resolveTheme } from './resolve'
export { serializeThemeCss } from './serialize'
export { applyTheme, clearTheme, themeVars } from './apply'
export { cssVar, formatVarName, kebabSegment, NAMESPACE_TABLE } from './naming'
export { legacyV0Alias } from './aliases/legacy-v0'
export { toDTCG, fromDTCG } from './dtcg/to-dtcg' /* + from-dtcg */
export { parseColor, formatColor } from './dtcg/color'
export { ThemeonError } from './errors'
export { isToken } from './types'
export type {
  Token, TokenType, TokenLeafInput, TokenTreeInput, Tokenized, TextStyleValue,
  WellKnownSys, SysTreeInput, SysPatch, ThemeDefinition, ThemeConfig,
  ResolvedTheme, ResolvedToken, ResolveOptions, NamingOptions, AliasesOption, AliasRule,
  SerializeCssOptions, ElementLike, CssVarRef, CssVarName, AutoComplete,
  ToDTCGOptions, DTCGExport, FromDTCGResult, ThemeonErrorCode,
} from /* соответствующие модули */
```

`parity.test.ts` — сценарий (русские описания):
1. Полное определение темы (палитра + все well-known группы + 2 темы + legacy-алиасы + prefix) → `resolveTheme`.
2. **Build-канал:** `serializeThemeCss` → распарсить собственный вывод примитивным regex (`/(--[\w-]+)\s*:\s*([^;]+);/g`) по блокам `:root` и `[data-theme="dark"]`.
3. **Runtime-канал:** `themeVars(resolved)` и `themeVars(resolved,'dark')` → применить `applyTheme` на фейковый el, собрать записанные пары.
4. Утверждения: множества имён И значения попарно идентичны в обоих каналах (база и тема), включая alias-пары (правило P1.4 шаг 8: алиасы входят в `vars`/`themeVars` — иначе runtime-переключение потеряло бы легаси-потребителей).
5. Кейс с `prefix: 'to'` — паритет сохраняется.

`api.test.ts`: `expect(Object.keys(await import('./index')).sort()).toMatchInlineSnapshot(...)` — runtime-экспорты; типовые проверяются typecheck'ом.

`README.md` (английский): что это (1 абзац из master §1), install, quickstart (define → resolve → serialize + apply), таблица опций resolve/serialize, DTCG-мост (2 предложения + ссылка на спеку), предупреждение про `@custom-media` (не Baseline), статус «pre-1.0, API may change».

package.json дополнить: `"description"`, `"keywords": ["design-tokens","css-variables","theming","dtcg","design-system"]`, `"repository"`, `"homepage"` (репо GitHub, если уже создан remote — взять из git; нет remote — placeholder `https://github.com/<org>/themeon` с Pending Work-пометкой).
**Validation:** `pnpm --filter @themeon/core test` — зелёный (parity + api + все предыдущие); `pnpm --filter @themeon/core build && pnpm check:pack` — publint/attw чисто; `pnpm test -- --coverage` (или эквивалент фильтра) — statements по `packages/core/src` ≥ 85%; `node -e "const p=require('./packages/core/package.json'); process.exit(p.dependencies?1:0)"` — deps пусто; `pnpm lint`/`typecheck` — чисто; в `src/index.ts` нет `THEMEON_CORE_STUB`.
**Deliverables:** финальный публичный API с freeze-snapshot, parity-тест, README+JSDoc, зелёные пакетные гейты, покрытие ≥85%.
**Completion Notes:** `src/index.ts` переписан: stub-контент P0.3 (`THEMEON_CORE_STUB`) снесён, собраны все публичные экспорты из реальных модулей (`defineTokens`/`defineTheme`, `resolveTheme`, `serializeThemeCss`, `applyTheme`/`clearTheme`/`themeVars`, `cssVar`/`formatVarName`/`kebabSegment`/`NAMESPACE_TABLE`, `legacyV0Alias`, `toDTCG`/`fromDTCG`, `parseColor`/`formatColor`, `ThemeonError`, `isToken` + все типы по списку Code Guidance; `internal/`, `walkTree`, `TOKEN_BRAND`, DTCG-типы документа НЕ экспортируются). `index.ts` — только реэкспорты, без логики. Создан `src/parity.test.ts` (4 теста): полное определение темы (палитра + 13 well-known групп + 2 темы + legacy-алиасы) резолвится один раз, затем build-канал (`serializeThemeCss` → парс собственного вывода regex `/(--[\w-]+)\s*:\s*([^;]+);/g` по блокам `:root`/`[data-theme]`) и runtime-канал (`themeVars`→`applyTheme` на фейковый `ElementLike`) сверяются на попарное равенство имён и значений для базы и каждой темы; кейсы `aliases:'legacy-v0'`, без алиасов, `prefix:'to'`, `refLayer:'inline'`. Создан `src/api.test.ts` (2 теста): `toMatchInlineSnapshot` списка из 18 рантайм-экспортов (freeze) + проверка отсутствия `THEMEON_CORE_STUB`. Удалён `src/index.test.ts` (тестировал снесённый stub). Шесть главных экспортов получили английский JSDoc `@example` в своих модулях (`define.ts` ×2, `resolve.ts`, `serialize.ts`, `apply.ts`, `dtcg/to-dtcg.ts`). Создан `packages/core/README.md` (английский): назначение, install, quickstart (define→resolve→serialize+apply), таблицы опций `resolveTheme`/`serializeThemeCss`, DTCG-мост, предупреждение про non-Baseline `@custom-media`, статус pre-1.0. `packages/core/package.json` дополнен `description`/`keywords`(`design-tokens`,`css-variables`,`theming`,`dtcg`,`design-system`)/`repository`/`homepage` (URL — placeholder `github.com/themeon/themeon` до GitHub-remote); `sideEffects: false` подтверждён; `dependencies` отсутствует. Команды (все зелёные): `pnpm --filter @themeon/core typecheck` — чисто; `test` — 13 файлов / 156 тестов; `pnpm lint` (oxlint) — exit 0; `build` — зелёный (dist/index.js вырос 1.86 kB → 63.20 kB: stub снят, реальные модули теперь в бандле — прямое свидетельство сноса stub); `check:pack` — publint «All good!» + attw `--profile esm-only` node16/bundler 🟢; покрытие (`vitest --coverage`, v8) — statements 90.39% (`packages/core/src` ≥ 85% выполнено); `node -e` deps-проверка — `dependencies: undefined`; `src/index.ts` не содержит `THEMEON_CORE_STUB`. Коммит `68d168d`.
**Pending Work:** `repository`/`homepage` в `packages/core/package.json` — placeholder `https://github.com/themeon/themeon` до появления GitHub-remote (снять при создании remote, до релиза P4+).
**Known Deviations:** (1) `@example`-JSDoc шести главных экспортов добавлен в их исходные модули (`define.ts`/`resolve.ts`/`serialize.ts`/`apply.ts`/`dtcg/to-dtcg.ts`), а не в `index.ts` — файлы вне буквального списка Files item'а. Причина: `index.ts` по Implementation Rule «только реэкспорты, никакой логики», а JSDoc на `export { x } from './y'` не привязывается к символу идиоматично; Implementation Rule «каждый публичный экспорт имеет английский JSDoc с @example» выполним только на объявлениях. Изменения — чисто документирующие (без правки поведения), объявления уже несли английский JSDoc из P1.2–P1.7. (2) Удалён `src/index.test.ts` (не в списке Files) — его единственный тест проверял `THEMEON_CORE_STUB`, снесённый этим item'ом; оставить = красный build. Замещён `api.test.ts`. (3) `repository`/`homepage` — placeholder-URL (нет git-remote) — предусмотрено Code Guidance item'а («нет remote — placeholder … с Pending Work-пометкой»), вынесено в Pending Work.
**Escalation Needed:** no

## Phase Handoff

**Сдано (P1.1–P1.8, 8/8 терминальны):** `@themeon/core` — L0-ядро токен-движка с zero runtime deps (`dependencies` отсутствует в `packages/core/package.json`). Публичный API заморожен `src/api.test.ts` (18 рантайм-экспортов) и собран в едином entry `.` (`src/index.ts`, stub P0.3 снесён). Полная цепочка: `defineTokens`/`defineTheme` (авторинг, leaf-wrapping typed-ссылок, P-D12) → `resolveTheme` (схлопывание цепочек, циклы/коллизии = ошибки, naming ровно один раз P-D14, refLayer P-D13, breakpoints-канал) → два канала-выхода: `serializeThemeCss` (static CSS: `@layer`/`:root`/`[data-theme]`/`@custom-media`/`@property`, D4/D6/D8) и `applyTheme`/`clearTheme`/`themeVars` (runtime). Naming engine (`naming.ts`) — единственный kebab/namespace-трансформер пакета (Tailwind-v4 namespaces + double-dash companion для text, D5); `legacyV0Alias` — rule-based мост к донорским именам (снос P5). DTCG-мост `toDTCG`/`fromDTCG` (Format 2025.10 multi-file + resolver.json, P-D15) + микро-парсер цвета `parseColor`/`formatColor`. Паритет build/runtime — по построению (общий `ResolvedTheme`) И по тестам (`parity.test.ts`: имена+значения побайтово равны в обоих каналах для базы и тем, включая alias-пары).

**Финальные гейты фазы (зелёные, 2026-07-12):** `pnpm --filter @themeon/core lint && typecheck && test && build` — 13 файлов / 156 тестов, oxlint exit 0, dist/index.js 63.20 kB; `pnpm check:pack` — publint «All good!» + attw esm-only 🟢; покрытие statements 90.39% (≥85%); `dependencies` пусто.

**Отклонения от ТЗ (сводно):** несколько item'ов закрыты 🟠 Done with deviations — детали в Known Deviations каждого; все отклонения деталь-уровня (не архитектурные), инварианты D2/D4/D5/D12/D14 сохранены. Открытый Pending Work по фазе: placeholder `repository`/`homepage` в package.json (до GitHub-remote).

**Вход для P2 (`@themeon/css`):** потребляет naming-канон — `cssVar(token)`/`formatVarName(path)`/`NAMESPACE_TABLE` (comp-CSS ссылается на `var(--…)` этими именами); значения токенов в P1 — авторские строки, полный color-инжиниринг (seed→шкала, APCA) — `@themeon/colors` в P2.
**Вход для P3 (`@themeon/vue`/`nuxt`/`vite`):** `ResolvedTheme` (плоский `vars`, `themes`, `breakpoints`, `schemes`) — вход `useTheme`; `applyTheme`/`clearTheme`/`themeVars` — runtime-переключение (SSR-нейтральны, структурный `ElementLike`); `serializeThemeCss` — источник анти-FOUC статики; управление `data-theme`/persist/prefers-color-scheme — целиком P3.
