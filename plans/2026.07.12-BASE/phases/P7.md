# P7 — Backlog: registry пресетов, адаптеры стадии 2, Vue-обёртки, composer Blade

## Phase Context

Низкоприоритетный backlog, без реального потребителя сегодня (`00_MASTER_PLAN.md` §5 D11,
§6.4–§6.6, §7 риск «overengineering соло-пакета: registry/composer — только под реального
консьюмера»). По решению пользователя (P-D47) фаза спроектирована **облегчённо**: каждый item —
trigger-gated заготовка (условие появления живого консьюмера + архитектурный принцип +
куда класть код), БЕЗ полного Definition-of-Detailed (файловые деревья/эталонный код/точные
API-сигнатуры/RAG на версии библиотек) — осознанное отклонение от общего DoD-канона Design
Program (`plan.md` §2а.4), т.к. полная детализация 6 спекулятивных фич до появления
потребителя противоречит риск-таблице мастер-плана и Execution Rules (не изобретать заранее).
Когда триггер конкретного item'а сработает — ПЕРЕД исполнением обязателен повторный
`/task:plan-design 2026.07.12-BASE P7.m` (углубление до полного DoD + свежий RAG на актуальные
версии/API на тот момент: сегодняшние факты о Bootstrap/Vuetify/PrimeVue устареют за время
простоя backlog). Модель дизайна — sonnet/high (`plan.md` §3 Routing «Design-стадия по
фазам», P-D17: backlog — низкий архитектурный риск). Q4 (`open-questions.md`, генерализация
пакета по находкам живых пилотов P5 — `varMap`/`roles`, `aliases`/`refLayer`, breakpoints-мост)
в эту фазу НЕ входит — по решению пользователя остаётся отдельным Exploration-вопросом,
кандидатом в отдельную фазу после закрытия P5.

**Scope Included (фаза):** держать 6 trigger-gated backlog-заготовок актуальными и честно
отражёнными в Status Board (⬜ = «планировали, не сделали»); при срабатывании триггера любого
item'а — повторный `/task:plan-design 2026.07.12-BASE P7.m` до полного DoD + свежий RAG.
**Scope Excluded (фаза):** любая реализация кода сегодня; полный DoD 6 спекулятивных фич без
живого потребителя (overengineering — §7 риск-таблицы, §6.5 «Vue — не v1»); ⛔/🟢-закрытие
item'ов ради архивации плана (P-D73); Q4-генерализация (отдельный Exploration).

**Подтверждение 2026-07-15 (P-D73, owner-решение).** Фаза сверена по прямому принципу владельца
(«убрать пункт можно ТОЛЬКО если он уже сделан или больше не вписывается; иначе — не закрывать»).
Repo-grounded аудит каждого item'а на HEAD: ни один из 6 не «уже сделан» (в `packages/` из
адаптеров только `naive`; CLI-подкоманды — `schema/init/build/check`, нет `add`; SFC-обёрток
примитивов нет; `composer.json`/`.blade.php` нет) и ни один не «больше не вписывается»
(P7.5 §6.5 — off-by-default, но НЕ архитектурно запрещён). **Вывод (на тот момент):** все 6
остаются ⬜ живым backlog'ом; план остаётся 🟡 In progress и НЕ архивируется до срабатывания
триггеров ИЛИ отдельного явного решения владельца. §3 Routing exec-таблица: P7.1–P7.6 несут
`Exec = plan-design` (RAG:— repo-grounded — сверка на HEAD 2026-07-15).

**Реконсиляция 2026-07-15 (`P-D79`, отдельное явное owner-решение — предусмотрено абзацем выше
и Phase Handoff `plan-close`).** Все 6 items перенесены из тяжёлого 16-полевого формата
исполняемого плана в лёгкий `ROADMAP.md` (корень репозитория) — статус каждого item'а меняется
на `⛔ Skipped by decision` **относительно ЭТОГО плана/формата**, не относительно самой идеи:
ни триггер, ни намерение не отменяются (P-D73 запрещает именно фиктивное закрытие
«планировали → сделаем вид, что не нужно» — здесь другое: формат трекинга сменился на более
уместный для спекулятивной работы без активного триггера, содержание идей сохранено дословно
в роадмапе). С этим решением фаза P7 терминальна (6/6 `⛔`), что открывает архивацию плана
целиком (`/task:plan-close archive 2026.07.12-BASE`).

## Phase Status

| ID | Title | Status | Updated |
|:--|:--|:--|:--|
| P7.1 | Registry пресетов тем (shadcn-style `registry-item.json`) — релоцирован в `ROADMAP.md` | ⛔ Skipped by decision | 2026-07-15 |
| P7.2 | Адаптер стадии 2 — Bootstrap — релоцирован в `ROADMAP.md` | ⛔ Skipped by decision | 2026-07-15 |
| P7.3 | Адаптер стадии 2 — Vuetify — релоцирован в `ROADMAP.md` | ⛔ Skipped by decision | 2026-07-15 |
| P7.4 | Адаптер стадии 2 — PrimeVue — релоцирован в `ROADMAP.md` | ⛔ Skipped by decision | 2026-07-15 |
| P7.5 | Vue-обёртки примитивов (`<ToStack>` и т.п.) — релоцирован в `ROADMAP.md` | ⛔ Skipped by decision | 2026-07-15 |
| P7.6 | Composer-пакет Blade-компонентов (FlexCMS) — релоцирован в `ROADMAP.md` | ⛔ Skipped by decision | 2026-07-15 |

### P7.1 — Registry пресетов тем (shadcn-style `registry-item.json`)

**Status:** ⛔ Skipped by decision
**Intent:** Дать новым потребителям пакета готовые, устанавливаемые копированием пресеты тем
(набор `*.theme.ts` + сопутствующих файлов) через CLI-подкоманду, по аналогии с
`shadcn add` / `registry-item.json`.
**Why:** Снижает трение первого запуска для потребителей за пределами dterema/vintera/Flex*.
Пока нет ни одного такого стороннего потребителя — ценность не подтверждена использованием,
поэтому item задизайнен только на уровне триггера и принципа, не реализации.
**Scope Included:** (после триггера) CLI-подкоманда `themeon add <preset>` в существующем
`themeon`-бинарнике; JSON-манифест пресета (name/type/files/dependencies — форма проверяется
по актуальному shadcn-формату на момент триггера, RAG обязателен); минимум 1 пресет, собранный
из факта реального стороннего консьюмера (не выдуманный «для примера»).
**Scope Excluded:** Сегодня — любая реализация кода. После триггера — хостинг/сервер registry
(статический JSON в репозитории/CDN — решается на месте, не проектируется заранее); установка
пресета как npm-зависимости (нужна копия для кастомизации потребителем, не пакет).
**Inputs:** —
**Files:** — (не спроектировано; при триггере — новый модуль в `packages/cli/src/`)
**Required Reads:** `00_MASTER_PLAN.md` §5 D11, §6.5–§6.6, §7 (риск overengineering) ·
`packages/cli/src/cli.ts` + `packages/cli/src/templates.ts` (существующий citty-каркас
CLI, P-D28 — новая подкоманда обязана лечь в него, не в отдельный бинарник) ·
`plans/2026.07.12-BASE/phases/P4.md` P4.6 (паттерн e2e API-freeze фикстуры для CLI).
**Implementation Rules:** Триггер item'а — появление ≥1 реального стороннего потребителя
пакета (не dterema/vintera/Flex*), запросившего готовый пресет темы. До триггера статус
остаётся ⬜, item не трогается. При срабатывании триггера design-сессия ОБЯЗАНА: (1) RAG на
актуальный формат shadcn `registry-item.json` (дрейфует со временем); (2) перепроверить, что
citty (`packages/cli`) остаётся каркасом CLI на тот момент; (3) выдать полный DoD (файловое
дерево, TS-сигнатуры, эталонный код) — сегодняшний облегчённый текст DoD не заменяет.
**Code Guidance:** Пресет — не npm-зависимость, а шаблон, устанавливаемый копированием файлов
в проект потребителя (тот же принцип, что у shadcn `add`: код становится собственностью
потребителя и открыт для правки). Реализация — новая подкоманда в уже существующем
`themeon`-CLI (citty, `packages/cli`), а не отдельный пакет/бинарник.
**Validation:** (при триггере) стандартный набор CLI-пакета — `typecheck`/`test`/`lint`/
`build`/`check:pack` зелёные; e2e API-freeze фикстура (паттерн P4.6) расширяется на новую
подкоманду.
**Deliverables:** —
**Completion Notes:** Перенесено в `ROADMAP.md` (репозиторий, корень) — 2026-07-15, `P-D79`,
owner-решение. **НЕ абандон**: идея/триггер сохранены дословно в роадмапе, только формат
трекинга сменился (тяжёлый 16-полевой item исполняемого плана → лёгкий пункт роадмапа —
уместнее для спекулятивной, ещё не спроектированной работы без активного триггера). Статус
`⛔ Skipped by decision` относится к ЭТОМУ плану/формату, не к самой идее.
**Pending Work:** —
**Known Deviations:** —
**Escalation Needed:** no

### P7.2 — Адаптер стадии 2 — Bootstrap

**Status:** ⛔ Skipped by decision
**Intent:** Адаптер `@themeon/bootstrap`, транслирующий резолвнутую тему ThemeOn в
Bootstrap-переменные (Sass-переменные `$primary`/`$body-bg`/… либо CSS custom properties
Bootstrap 5.3+ — какой канал актуален, решается на триггере через RAG), по архитектуре
контракта `@themeon/naive` (D11: адаптер — сменная деталь, orphan-риск отдельной библиотеки
изолирован в свой пакет).
**Why:** В донорских/пилотных проектах (dterema/vintera) Bootstrap не найден (`00_MASTER_PLAN.md`
§6.6) — открытый вопрос пользователю «есть ли живой Bootstrap-проект-потребитель», приоритет
поднимается, если появится.
**Scope Included:** (после триггера) `packages/bootstrap` — `toNative()`-аналог
(`packages/naive/src/to-native.ts` как референс формы: вход `ResolvedTheme`, выход —
структура Bootstrap ожидает), README, тесты по образцу `@themeon/naive`
(`api.test.ts`/`to-native.test.ts`/`color.test.ts`).
**Scope Excluded:** Сегодня — любая реализация. Codemod разметки существующего
Bootstrap-проекта на классы ThemeOn (`@themeon/css`) — отдельный вопрос, не входит в
адаптер токенов.
**Inputs:** —
**Files:** —
**Required Reads:** `00_MASTER_PLAN.md` §6.4/§6.6, §7 · `packages/naive/src/{to-native.ts,
merge.ts, color.ts, types.ts}` (архитектурный референс: чистые pure-функции трансляции
`ResolvedTheme` → нативный формат UI-библиотеки, самостоятельное вычисление hover/pressed/
suppl-оттенков через colorjs.io — P-D29/P-D30, НЕ полагаться на деривацию самой библиотеки).
**Implementation Rules:** Триггер — появление реального Bootstrap-проекта-потребителя (свой
или сторонний). При срабатывании — RAG на актуальную версию Bootstrap и способ кастомизации
темы (Sass-переменные vs CSS custom properties — [UNVERIFIED] Bootstrap 5.3, по памяти,
ввёл `data-bs-theme`+CSS-переменные; не проверено RAG в эту сессию, авторитетный источник на
момент триггера может отличаться) + полный DoD.
**Code Guidance:** Тот же принцип, что `@themeon/naive` — pure-функции, zero UI-рантайм
зависимостей сверх `@themeon/core`, самостоятельный расчёт производных оттенков через
colorjs.io (не полагаться на деривацию Bootstrap/Sass), guard от `__proto__`/`constructor`
при любом deep-merge (`packages/naive/src/merge.ts` как образец).
**Validation:** (при триггере) `typecheck`/`test`/`lint`/`build`/`check:pack` зелёные по
образцу `packages/naive`; живой смок в проекте-потребителе.
**Deliverables:** —
**Completion Notes:** Перенесено в `ROADMAP.md` (репозиторий, корень) — 2026-07-15, `P-D79`,
owner-решение. **НЕ абандон**: идея/триггер сохранены дословно в роадмапе, только формат
трекинга сменился (тяжёлый 16-полевой item исполняемого плана → лёгкий пункт роадмапа —
уместнее для спекулятивной, ещё не спроектированной работы без активного триггера). Статус
`⛔ Skipped by decision` относится к ЭТОМУ плану/формату, не к самой идее.
**Pending Work:** —
**Known Deviations:** —
**Escalation Needed:** no

### P7.3 — Адаптер стадии 2 — Vuetify

**Status:** ⛔ Skipped by decision
**Intent:** Адаптер `@themeon/vuetify`, транслирующий резолвнутую тему ThemeOn в
Vuetify theme-конфиг ([UNVERIFIED] `ThemeDefinition` — `colors`/`variables`, по памяти, не
проверено RAG в эту сессию), той же архитектуры, что `@themeon/naive`.
**Why:** Кандидат стадии 2 (`00_MASTER_PLAN.md` §6.4) — приоритет зависит от появления
реального Vuetify-проекта-потребителя; сегодня такого нет.
**Scope Included:** (после триггера) `packages/vuetify` — трансляция `ResolvedTheme` →
`ThemeDefinition`, README, тесты по образцу `@themeon/naive`.
**Scope Excluded:** Сегодня — любая реализация. Vuetify-компонентные default-props
(`defaults` в `ThemeDefinition`) — не токен-адаптер, вне скоупа, если не решено иначе на
триггере.
**Inputs:** —
**Files:** —
**Required Reads:** `00_MASTER_PLAN.md` §6.4/§6.6, §7 · `packages/naive/src/{to-native.ts,
merge.ts, color.ts}` (архитектурный референс — см. P7.2 Required Reads, тот же принцип).
**Implementation Rules:** Триггер — появление реального Vuetify-проекта-потребителя.
При срабатывании — RAG на актуальную версию Vuetify и форму `ThemeDefinition`
(Vuetify 3 API может измениться) + полный DoD.
**Code Guidance:** Тот же принцип, что P7.2/`@themeon/naive` — pure-функции, самостоятельный
расчёт производных оттенков через colorjs.io, zero лишних рантайм-зависимостей, proto-guard
на merge.
**Validation:** (при триггере) `typecheck`/`test`/`lint`/`build`/`check:pack` зелёные по
образцу `packages/naive`; живой смок в проекте-потребителе.
**Deliverables:** —
**Completion Notes:** Перенесено в `ROADMAP.md` (репозиторий, корень) — 2026-07-15, `P-D79`,
owner-решение. **НЕ абандон**: идея/триггер сохранены дословно в роадмапе, только формат
трекинга сменился (тяжёлый 16-полевой item исполняемого плана → лёгкий пункт роадмапа —
уместнее для спекулятивной, ещё не спроектированной работы без активного триггера). Статус
`⛔ Skipped by decision` относится к ЭТОМУ плану/формату, не к самой идее.
**Pending Work:** —
**Known Deviations:** —
**Escalation Needed:** no

### P7.4 — Адаптер стадии 2 — PrimeVue

**Status:** ⛔ Skipped by decision
**Intent:** Адаптер `@themeon/primevue`, транслирующий резолвнутую тему ThemeOn в
PrimeVue-пресет темы ([UNVERIFIED] Styled Mode `definePreset`/`$dt`-токены, по памяти, не
проверено RAG в эту сессию — форма перепроверяется на триггере), той же архитектуры, что
`@themeon/naive`.
**Why:** `00_MASTER_PLAN.md` §6.4: PrimeVue — кандидат стадии 2, его собственная
токен-архитектура уже референс формы для будущих адаптеров ThemeOn. Приоритет — по
появлению реального PrimeVue-потребителя.
**Scope Included:** (после триггера) `packages/primevue` — трансляция `ResolvedTheme` →
PrimeVue preset, README, тесты по образцу `@themeon/naive`.
**Scope Excluded:** Сегодня — любая реализация. PrimeVue Unstyled-mode/PassThrough — не
токен-адаптер, вне скоупа, если не решено иначе на триггере.
**Inputs:** —
**Files:** —
**Required Reads:** `00_MASTER_PLAN.md` §6.4/§6.6, §7 · `packages/naive/src/{to-native.ts,
merge.ts, color.ts}` (архитектурный референс — см. P7.2 Required Reads, тот же принцип).
**Implementation Rules:** Триггер — появление реального PrimeVue-проекта-потребителя.
При срабатывании — RAG на актуальную версию PrimeVue и форму `definePreset`/design-token API
(может измениться между major) + полный DoD.
**Code Guidance:** Тот же принцип, что P7.2/P7.3/`@themeon/naive` — pure-функции,
самостоятельный расчёт производных оттенков через colorjs.io, zero лишних рантайм-зависимостей,
proto-guard на merge.
**Validation:** (при триггере) `typecheck`/`test`/`lint`/`build`/`check:pack` зелёные по
образцу `packages/naive`; живой смок в проекте-потребителе.
**Deliverables:** —
**Completion Notes:** Перенесено в `ROADMAP.md` (репозиторий, корень) — 2026-07-15, `P-D79`,
owner-решение. **НЕ абандон**: идея/триггер сохранены дословно в роадмапе, только формат
трекинга сменился (тяжёлый 16-полевой item исполняемого плана → лёгкий пункт роадмапа —
уместнее для спекулятивной, ещё не спроектированной работы без активного триггера). Статус
`⛔ Skipped by decision` относится к ЭТОМУ плану/формату, не к самой идее.
**Pending Work:** —
**Known Deviations:** —
**Escalation Needed:** no

### P7.5 — Vue-обёртки примитивов (`<ToStack>` и т.п.)

**Status:** ⛔ Skipped by decision
**Intent:** Тонкие Vue SFC-обёртки над CSS-классами composition-примитивов `@themeon/css`
(`<ToStack>`, `<ToCluster>` и т.п.) — синтаксический сахар поверх уже существующих классов
(`.stack`/`.cluster`/…, `packages/css`, фаза P2).
**Why:** `00_MASTER_PLAN.md` §6.5 — **явная рекомендация НЕ делать это в v1**: пакет
позиционируется как «утилиты, не UI-kit»; Vue-компоненты превратили бы ThemeOn в
компонентную библиотеку, конкурирующую с Naive/Vuetify/PrimeVue вместо их темизации.
Item существует как backlog-заготовка на случай сильного сигнала боли от реального
потребителя, а не как одобренное направление по умолчанию.
**Scope Included:** (после триггера, и только если триггер — весомый) тонкие SFC-обёртки,
единственная функция которых — навесить нужный класс/custom-property-параметры на элемент;
без собственной логики/состояния.
**Scope Excluded:** Сегодня — любая реализация. По умолчанию — НЕ делать (§6.5); триггер
обязан включать явное обоснование, почему одних CSS-классов (уже покрывают Blade/plain HTML/
любой фреймворк) недостаточно именно для этого потребителя.
**Inputs:** —
**Files:** —
**Required Reads:** `00_MASTER_PLAN.md` §6.5 (решение «нет v1» и его причина) ·
`packages/css/src` (composition-классы, фаза P2, `phases/P2.md`) — обёртки лишь навешивают
существующие классы, не изобретают новый CSS-контракт.
**Implementation Rules:** Триггер — конкретный реальный потребитель, который явно
сформулировал, почему CSS-классов недостаточно (не гипотетическое удобство). До триггера
item остаётся ⬜ и по умолчанию отклоняется при пересмотре backlog. При срабатывании —
дизайн-сессия обязана явно зафиксировать в Decision Log, почему §6.5 пересматривается
(новый факт, не молчаливая отмена).
**Code Guidance:** Обёртка не хранит состояния и не дублирует CSS-контракт — прокидывает
`props` в `style`/`class` существующих примитивов `@themeon/css`, ничего не решает
самостоятельно поверх них.
**Validation:** (при триггере) `typecheck`/`test`/`lint`/`build` зелёные; визуальный паритет
с чистыми CSS-классами (снапшот/скриншот-сверка).
**Deliverables:** —
**Completion Notes:** Перенесено в `ROADMAP.md` (репозиторий, корень) — 2026-07-15, `P-D79`,
owner-решение. **НЕ абандон**: идея/триггер сохранены дословно в роадмапе, только формат
трекинга сменился (тяжёлый 16-полевой item исполняемого плана → лёгкий пункт роадмапа —
уместнее для спекулятивной, ещё не спроектированной работы без активного триггера). Статус
`⛔ Skipped by decision` относится к ЭТОМУ плану/формату, не к самой идее.
**Pending Work:** —
**Known Deviations:** —
**Escalation Needed:** no

### P7.6 — Composer-пакет Blade-компонентов (FlexCMS)

**Status:** ⛔ Skipped by decision
**Intent:** Отдельный Composer-пакет с Blade-компонентами/директивами, обёрнутыми вокруг
CSS-каркаса `@themeon/css` (site-blueprints/components, фаза P2) и, при готовности, канала
доставки P6 (Laravel+Vite рецепт, `serializeThemePatch`), под будущего потребителя FlexCMS.
**Why:** `Related` в Meta плана называет FlexCMS потребителем стадии 2; готового FlexCMS-факта
интеграции сегодня нет — item ждёт, пока FlexCMS реально начнёт подключать ThemeOn.
**Scope Included:** (после триггера) Composer-пакет (namespace/`composer.json`,
service-provider публикации view-компонентов), Blade-компоненты как тонкие обёртки над
CSS-классами `@themeon/css` (по аналогии с ограничением P7.5 — не изобретать новую логику).
**Scope Excluded:** Сегодня — любая реализация. Пока фаза P6 (Laravel-канал, `packages/
themeon` рецепт+пример) не терминальна — Composer-пакет на неё не полагается автоматически;
на триггере решить, что фаза P6 обязана быть закрыта первой.
**Inputs:** —
**Files:** —
**Required Reads:** `00_MASTER_PLAN.md` §8 (стадии: P6 → P7) · `plans/2026.07.12-BASE/
phases/P6.md` (Laravel-канал — как только детализирован; сегодня скелет) · `packages/css/src`
(CSS-контракт, который Blade-компоненты оборачивают).
**Implementation Rules:** Триггер — FlexCMS (или другой Laravel-потребитель) реально
начинает интеграцию ThemeOn. Предусловие — фаза P6 терминальна (Laravel-канал доставки токенов
существует) до старта реализации этого item'а, иначе Composer-пакет не на что опереться.
При срабатывании — RAG на актуальную версию Composer/Laravel-конвенций публикации view-пакетов
+ полный DoD.
**Code Guidance:** Blade-компоненты — тонкие обёртки над CSS-классами `@themeon/css`
(тот же принцип, что P7.5 для Vue) — без собственной бизнес-логики; токены доставляются
Laravel-каналом P6, не заново.
**Validation:** (при триггере) Composer `validate`/тесты пакета (PHPUnit или аналог, решается
на месте) зелёные; живой смок в FlexCMS/примере из P6.
**Deliverables:** —
**Completion Notes:** Перенесено в `ROADMAP.md` (репозиторий, корень) — 2026-07-15, `P-D79`,
owner-решение. **НЕ абандон**: идея/триггер сохранены дословно в роадмапе, только формат
трекинга сменился (тяжёлый 16-полевой item исполняемого плана → лёгкий пункт роадмапа —
уместнее для спекулятивной, ещё не спроектированной работы без активного триггера). Статус
`⛔ Skipped by decision` относится к ЭТОМУ плану/формату, не к самой идее.
**Pending Work:** —
**Known Deviations:** —
**Escalation Needed:** no

## Phase Handoff

**Фаза P7 ЗАКРЫТА 2026-07-15** (`/task:plan-close 2026.07.12-BASE reconcile` в рамках
`P-D79` — отдельное явное owner-решение, предусмотренное собственным Phase Handoff'ом фазы
от 2026-07-15, см. историю коммитов) — 6/6 items терминальны, все `⛔ Skipped by decision`.
Это **релокация формата, не отмена содержания**: все 6 идей и их триггеры перенесены
дословно в корневой `ROADMAP.md` — там backlog остаётся видимым и живым, просто без тяжёлого
16-полевого формата исполняемого плана, который не подходит для полностью спекулятивной,
ещё не триггернутой работы.

- **История до релокации** (design-сессия 2026-07-15, P-D73): подтверждён trigger-gated design
  (P-D47 в силе); repo-grounded сверка каждого из 6 items на HEAD (ни один не сделан, ни один
  не устарел) → на тот момент решено НЕ закрывать ⛔/🟢 ради архивации, план оставался
  🟡 In progress.
- **Релокация 2026-07-15 (P-D79)**: тот запрет был про фиктивное закрытие («сделаем вид, что
  не нужно») — здесь другое основание: содержание идей и триггеров сохранено 1:1 в
  `ROADMAP.md`, сменился только артефакт трекинга. `⛔` относится к формату ЭТОГО плана, не
  к идеям.
- **Триггеры (без изменений, актуальны в `ROADMAP.md`)**: P7.1 — ≥1 реальный сторонний
  потребитель, запросивший готовый пресет; P7.2/P7.3/P7.4 — реальный Bootstrap/Vuetify/
  PrimeVue-проект-потребитель; P7.5 — потребитель с явным обоснованием, почему CSS-классов
  недостаточно (§6.5 по умолчанию отклоняет); P7.6 — FlexCMS/Laravel-потребитель начинает
  интеграцию (предусловие P6 терминальна — выполнено).
- **Влияние на план**: фаза P7 теперь терминальна (6/6 ⛔) → все фазы плана терминальны →
  план закрывается целиком, `/task:plan-close archive 2026.07.12-BASE` следующим шагом.
