# HANDOFF — 2026-07-14 — after P8.12

**Next:** Исполнить **P8.13 — `themeon` CLI: пороги из SSOT, исключения скана, рабочий скаффолд**
(тринадцатый item фазы, предпоследний). ТЗ детерминировано `findings/P8-css-layers-cli-checks.md`
§3 (SSOT-числа), §4 (канон исключений скана, воспроизведение) + `findings/P8-nuxt-vue-runtime.md`
§2 (почему тема не должна жить в rootDir) + `phases/P8.md` P8.13 целиком. После коммита —
ОБЯЗАТЕЛЬНЫЙ adversarial-review (opus, P-D51).

| Параметр | Значение |
|:--|:--|
| Model | **sonnet** |
| Thinking | **medium** (пинится самим `/task:plan-exec`) |
| Context | **continue (/clear) — manual item** |
| Суть | `packages/cli/src/checks/contrast.ts`: заменить локальную таблицу порогов/пар на импорт `checkThemeContrast`/`SEMANTIC_CONTRAST_PAIRS` из `@themeon/colors` (P8.6 SSOT) — CLI сейчас гоняет все пары как `usage:'body'` (порог 75), а гейт генерации — как `'text'` (60), из-за чего дефолтная тема пакета ПРОВАЛИВАЕТ собственный `themeon check`. `packages/cli/src/commands/check.ts`+`checks/hardcode.ts`: исключения скана — по ФАКТИЧЕСКИМ путям `--out`/`--tailwind`/`--config` + content-based skip по баннеру генератора (`/* generated by @themeon/… */`), удалить захардкоженный `EXCLUDE_FILE_RE` на имя `tokens.css` (Major #23 — сгенерированные файлы дают ложный hardcode и `unused=0` всегда). `packages/cli/src/commands/init.ts`: скаффолд темы в форме, которую реально грузит Nuxt-модуль (P8.4), дефолтное место — `theme/theme.config.ts` (не rootDir — иначе Nuxt теряет granular-HMR). Обязательный постоянный тест: дефолтная тема пакета проходит `themeon check`; связка `themeon init` → `nuxt dev` поднимается (slow-ярус, вместе с P8.4). |

```
/task:plan-exec 2026.07.12-BASE P8.13
```

**Cold-start reads (по порядку):**
1. `plans/2026.07.12-BASE/phases/P8.md` — Phase Context (инварианты фазы) + item **P8.13** целиком.
2. `plans/2026.07.12-BASE/findings/P8-css-layers-cli-checks.md` §3, §4 (SSOT-числа, канон исключений).
3. `plans/2026.07.12-BASE/findings/P8-nuxt-vue-runtime.md` §2 (место темы, granular-HMR).
4. `packages/cli/src/commands/check.ts` (текущий код).
5. `90_audit/AUDIT_2026-07-14_research-conformance.md` §«### 22», §«### 23».
6. `packages/core/src/dtcg/from-dtcg.ts` (P8.12, свежий импорт-канон — если P8.13 трогает
   что-то смежное с DTCG через CLI, но по Scope Included это не ожидается — только для контекста).
7. `plans/2026.07.12-BASE/plan.md` — §3 Routing, §5 Decision Log.

**Done:** (эта сессия)

- **P8.12 закрыт 🟢 Done.** `@themeon/core` `fromDTCG` — DTCG-импорт чужих бандлов по канону
  2025.10 (`findings/P8-dtcg-2025-10-canon.md` §7): корневой `$type` наследуется вниз (Major #8 —
  самый частый экспорт Tokens Studio `{"$type":"color","brand":{…}}` раньше импортировался
  пустым); multi-file бандл различает базу/темы в порядке (а) resolver-документ в бандле (детект
  по содержимому — `version==='2025.10'` + хотя бы одна запись `sets`/`modifiers` реальной формы
  `{sources:[...]}`/`{contexts:{...}}`, независимо от имени файла) → (б) явные `opts.base`/
  `opts.themes` → (в) эвристика подмножества путей (наибольшее число уникальных путей — база,
  подмножество — тема; коллизия нормализованных имён — warning+skip, не тихая перезапись) — Major
  #9 (чужой бандл `global.json`/`dark.json` раньше молча давал пустую тему). `$themes.json`/
  `$metadata.json` Tokens Studio распознаются и пропускаются. Fail-loud: пустой импорт → warning
  с диагнозом (`opts.onEmpty:'error'` → `ThemeonError`); токен без резолвимого `$type` → warning,
  тип НЕ угадывается по значению (§5.2.2 MUST NOT); `$value` рядом с дочерним non-`$` ключом →
  `ThemeonError` (§6.1 MUST). Новый публичный тип `FromDTCGOptions` (рантайм-сигнатура `fromDTCG`
  не меняла экспортов — `api.test.ts` не тронут). Коммит `1fcceb3`.
- **Adversarial-review (opus) по коммиту `1fcceb3`**: 3 подтверждённых находки, устранены
  коммитом `f2fce8a`: (1) эвристика подмножества путей молча затирала тему при коллизии
  нормализованных имён файлов (`dark.tokens.json`+`dark.json` → вторая тихо перезаписывала
  первую) — теперь warning+skip; (2) resolver-документ с malformed `sets[*].sources`/
  `modifiers[*].contexts[*].sources` (не массив) молча ронял токены этого сета/контекста —
  теперь warning; (3) тест round-trip (#16) не валидировал через `@terrazzo/parser`, как требует
  канон §9.16 — усилен реальной прогонкой парсера. Плюс не-блокирующая находка: `isLikelyResolver`
  мог ложно сработать на обычном токен-документе с группами, буквально названными
  `sets`/`modifiers` — детект ужесточён требованием реальной resolver-формы внутри записей.
  4 регресс-теста добавлены на все находки. Известная НЕ-регрессия вне скоупа (Pending Work
  `phases/P8.md` P8.12): `dtcgValueToRaw` для известного, но не DTCG-стандартного `$type`
  по-прежнему молча пропускает примитив без warning — асимметрично с новым поведением для
  отсутствующего `$type`, но это пре-P8.12 поведение, отдельная находка.
- Финальная валидация (все зелёные, после обоих коммитов): `pnpm build` (весь монорепо); `pnpm
  test` — 1201 тест; `pnpm typecheck` (11/11 пакетов); `pnpm lint` (oxlint) — чисто;
  `pnpm --filter @themeon/core test` отдельно — 185 тестов.

**Remaining:**

1. **P8.13–P8.14** — 2 items, порядок: P8.13 (CLI: SSOT-потребление + исключения скана + рабочий
   скаффолд) → P8.14 (research + финальная сверка фазы, закрывает P8 целиком).
   Каждый — `/task:plan-exec` (sonnet/medium) + ОБЯЗАТЕЛЬНЫЙ adversarial-review (opus/xhigh).
2. **P5.9 / P5.11 / P5.10** (пилоты) — ЗАБЛОКИРОВАНЫ до закрытия ВСЕЙ P8 (решение владельца
   2026-07-14).
3. **P6 / P7** — без изменений.

**Sources of truth:**

- План: `~/projects/packages/themeon/plans/2026.07.12-BASE/` (repo = SSOT; Vault — зеркало).
- Входы фазы P8 — `findings/P8-*.md`, НЕ `20_research/R-xx` (R-11 §1, R-13 §4.3/§4.4, R-14 §2.1 —
  ложные утверждения, P8.14 их размечает).
- Тест-стенд P8.1 — `tests/integration/` (4 яруса: `src/fast`, `src/browser`, `src/e2e`); `pnpm
  test:int`/`test:int:slow` не идут в `pnpm test` (отдельные команды).
- Пакеты: `~/projects/packages/themeon/packages/*` — HEAD (после коммита `f2fce8a`), дерево
  чистое. `dist` БРАТЬ В ПИЛОТЫ НЕЛЬЗЯ до закрытия P8.
- `@themeon/core` DTCG-мост целиком закрыт (P8.11 эмит + P8.12 импорт): `toDTCG(theme) →
  { files, warnings }`, `fromDTCG(files, opts?) → { definition, warnings }` — публичный контракт
  ломается свободно (P-D53), но КАЖДОЕ изменение рантайм-поверхности — в `api.test.ts` в том же
  item'е (в P8.11/P8.12 не потребовалось: рантайм-экспорты не менялись, только типы).
  `packages/core/src/dtcg/to-dtcg.ts` несёт `escapeDTCGSegment`/`toDTCGPath`/
  `pickResolverDefaultKey` — P8.12 их читает при реимпорте резолвер-документов (не переизобретает).
  `packages/core/src/dtcg/from-dtcg.ts` несёт `FromDTCGOptions` (`base?`/`themes?`/`onEmpty?`) —
  публичный тип-only экспорт.
- `packages/core/src/errors.ts` несёт `ThemeonErrorCode` с `'BAD_COLOR'` (P8.8),
  `'DTCG_NAME_COLLISION'` (P8.11) и переиспользованным `'DTCG_PARSE'` для новых ERROR-путей
  импорта (P8.12: пустой импорт при `onEmpty:'error'`, нерезолвимый resolver `$ref`, `$value`
  рядом с дочерним ключом) — публичные тип-only экспорты.
- `@themeon/colors` экспортирует `STEP10_DELTA` (P8.9, публичный, `scale.ts`).
- Changesets (@themeon/naive+@themeon/core P8.8; naive-деривация P8.9; DTCG-мост P8.11+P8.12) ещё
  не зарелижены (репо не в npm, P-D53) — P8.12 не заводила отдельный changeset (та же логика,
  что и P8.11).

**Open risks:**

- Тонкий APCA-запас (0.9–1.7 Lc) против `--color-bg-subtle` у пар `focusRing`/`link` в дефолт-теме
  (P8.7 review finding) — сдвиг нейтральной/акцентной шкалы `@themeon/colors` может увести их в FAIL;
  гейт fail-closed поймает на сборке, не сформирован как отдельный item.
- `@themeon/vite`: двойной инстанс `@themeon/core` в графе (наблюдение разведки P8.2, вне скоупа) —
  зафиксирована в Pending Work P8.2, отдельная проверка не заведена как item.
- P8.9 остаточный риск §3.5 (findings): в dark лестница `base→hover→pressed` идёт вверх по L ⇒
  контраст белых чернил падает 75.6→69.0→62.0 — порог `text` (60) держится, `body` (75) нет.
  Неизбежно при Radix-направлении и белых чернилах, не баг реализации.
- P8.10: `$theme` в `ComponentCustomProperties` — глобальная аугментация; потребитель, импортирующий
  `@themeon/vue`, но НЕ ставящий плагин, получит `$theme` типизированным (компилируется), но
  `undefined` в рантайме (неустранимо структурно, тот же паттерн у `$pinia`/`$router`, принят как
  остаточный риск).
- P8.11: имена тем с `.`/`{`/`}` не экранируются для имени файла/resolver-ключа контекста (review
  low finding, не спека-нарушение) — P8.12 читает такие имена файлов через собственную resolver/
  эвристику по содержимому (не парсит basename), риск снят для нормального потока.
- P8.12: `dtcgValueToRaw` для известного, но НЕ DTCG-стандартного `$type` (напр. `"border"`) молча
  пропускает примитивный `$value` без warning (§8.8 запрещает кастомные `$type` вообще) —
  асимметрично с новым поведением для ПОЛНОСТЬЮ отсутствующего `$type` (который теперь warns).
  Пре-P8.12 поведение, не регрессия этого item'а — отдельная находка, не заведена как item.

**Workarounds / Deferred / Open questions:**

- **workarounds:** `spawnNuxtDev` выбирает порт сам и ждёт HTTP-поллингом вместо парсинга stdout
  (P8.1 Known Deviation) — специфика этой среды исполнения, пересмотреть при переносе в CI.
- **deferred:** дубль таблицы пар в CLI `checks/contrast.ts` (→ P8.13, следующий item); `dispose()`
  у `UseThemeReturn` (P3.8, повторно deferred в P8.10 Scope Excluded); перевод русских JSDoc
  публичных типов на английский (репо-широкий долг); структурный DTCG-эмит `shadow`/`gradient`
  (backlog, P7); `@themeon/vite`: двойной инстанс core; `experimental.bundledDev` (Vite 8.1.x,
  открытый апстрим-баг); CJS-тем поддержка/документация (P8.4 review finding); e2e
  granular-vs-restart различение (P8.4 review finding); тонкий APCA-запас против `bg-subtle`
  (P8.7 review finding, см. Open risks); «жёлтая полоса» APCA для warning-заливок (P8.8, findings
  §4.2 — Q владельцу); экранирование имён тем с `.`/резолвер-ключей (P8.11 review low finding);
  `escapeDTCGSegment` не разделяет по `_` в отличие от naming-движка (P8.11 review nit); импорт
  сложных чужих resolver'ов с несколькими modifier'ами (P8.12 Scope Excluded, есть warning);
  `dtcgValueToRaw` не warn'ит на известном-но-нестандартном `$type` (P8.12 review finding, см.
  Open risks).
- **open_questions:** `open-questions.md` — Q3 (`@bg-dev/nuxt-naiveui`, нужен владелец до merge
  веток пилотов), Q4 (генерализация — частично поглощена P8). Q1/Q2/Q5 закрыты.
