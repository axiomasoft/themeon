# HANDOFF — 2026-07-14 — after P8.13

**Next:** Исполнить **P8.14 — Правка research + superseded-решения + финальная сверка фазы**
(последний item фазы P8; закрывает всю фазу). ТЗ детерминировано `phases/P8.md` P8.14 целиком —
пометить ложные утверждения в `20_research/R-11/R-13/R-14` как «ОПРОВЕРГНУТО», завести
superseded-цепочки для P-D26/P-D29/P-D31 в `plan.md` §5, дописать честные Known Deviations в
`phases/P4.md` P4.1 (аудит #28), прогнать все 6 гейтов + `plan-lint` на чистом дереве, закрыть фазу.
После коммита — ОБЯЗАТЕЛЬНЫЙ adversarial-review (opus, P-D51).

| Параметр | Значение |
|:--|:--|
| Model | **sonnet** |
| Thinking | **medium** (пинится самим `/task:plan-exec`) |
| Context | **continue (/clear) — manual item** |
| Суть | Пометить ЛОЖНЫЕ утверждения в `R-11`§1/`R-13`§4.2-4.4/§6.4/`R-14`§1.3/§2.1/§2.2 как «ОПРОВЕРГНУТО (2026-07-14, P8)» со ссылкой на `findings/P8-*.md` (не удалять — research это история); `plan.md` §5 — новые D# «supersedes P-D26/P-D29/P-D31»; `phases/P4.md` P4.1 Known Deviations — честно зафиксировать расхождение «ТЗ требовало исключать double-dash companion, код включает» (аудит #28), статус item'а не менять; грепом проверить, что каждый рецепт в README пакетов покрыт интеграционным тестом (нет теста → рецепт убрать); `open-questions.md` Q4 — обновить статус (частично поглощена P8). Финал: `pnpm build && pnpm lint && pnpm typecheck && pnpm test && pnpm test:int && pnpm check:pack` — всё зелёное, `plan-lint.py` без новых ошибок — фаза P8 закрывается целиком. |

```
/task:plan-exec 2026.07.12-BASE P8.14
```

**Cold-start reads (по порядку):**
1. `plans/2026.07.12-BASE/phases/P8.md` — Phase Context (инварианты фазы) + item **P8.14** целиком.
2. `90_audit/AUDIT_2026-07-14_research-conformance.md` — раздел «Системный вывод».
3. `plans/2026.07.12-BASE/findings/P8-*.md` — сводки (TL;DR/вердикты каждого файла).
4. `plans/2026.07.12-BASE/plan.md` — §5 Decision Log, записи P-D26/P-D29/P-D31.
5. `plans/2026.07.12-BASE/phases/P4.md` — P4.1 Completion Notes (что именно там неправда, аудит #28).
6. `plans/2026.07.12-BASE/20_research/{R-11,R-13,R-14}*.md` — участки, помечаемые опровергнутыми.

**Done:** (эта сессия)

- **P8.13 закрыт 🟠 Done with deviations.** `themeon` CLI: `checks/contrast.ts` переведён на SSOT
  `checkThemeContrast`/`SEMANTIC_CONTRAST_PAIRS` (`@themeon/colors`, P8.6) — локальная 3-парная
  таблица (всё `usage:'body'`) удалена, Major #22 закрыт структурно (CLI и `gen-tokens.mjs` читают
  один SSOT, паритет тестами P8.6). `commands/check.ts`: `scanIgnorePatterns` — дефолты
  (`node_modules`/`dist`/`.output`/`.nuxt`) + фактические `--out` (новый флаг, default
  `tokens.css`)/`--tailwind`/`config` + новый `--ignore`; content-based skip по
  `GENERATED_BANNER_RE` (регистронезависимый префикс `generated by @themeon/`, ловит оба
  генератора без флагов); пустой скан после исключений → `warning: no sources scanned` вместо
  тихого «зелёного». `checks/hardcode.ts`: `EXCLUDE_FILE_RE` снесён (Major #23 — ловил только
  буквальное имя `tokens.css`, легальный `--out` вида `src/styles/theme.css` протекал;
  `coverage.ts` вообще не исключал ничего — рассинхрон между линтерами); исключения теперь
  принадлежат сканеру (`commands/check.ts`), не отдельному линтеру. `commands/init.ts`
  скаффолдит `theme/theme.config.ts`, НЕ rootDir (`findings/P8-nuxt-vue-runtime.md` §2 —
  тема в корне лишает Nuxt-модуль granular CSS-HMR); новый `packages/cli/src/constants.ts`
  (`DEFAULT_THEME_CONFIG_PATH`) — общий дефолт для `init`/`build`/`check`.
  Тесты: `packages/cli/src/scaffold.test.ts` (новый) — `init → build(--out/--tailwind) → check`
  на РЕАЛЬНОМ скаффолде (Code Guidance запрещает синтетику здесь) — ручной Validation-сценарий,
  плюс сфабрикованный dead-ref/hardcode на обычном файле, плюс тот же прогон без `--out`/
  `--tailwind` (только баннер). `tests/integration/src/e2e/cli-init-nuxt.test.ts` (новый,
  `int-e2e`) — `runInit` → живой `nuxt dev` → 200 + `.nuxt/themeon-tokens.css` содержит
  `--color-bg-page` (регресс-гейт формы скаффолда). Коммит `04aec27` (реализация) + `793b384`
  (пост-review фикс, см. ниже).
  Финальная валидация (на `04aec27`, до review-фикса): `pnpm build` зелёно (11 пакетов, `@themeon/css` APCA-гейт
  пройден); `pnpm typecheck` 11/11 + `tests/integration`; `pnpm lint` (oxlint) чисто; `pnpm test`
  1203 теста/48 файлов; `pnpm test:int` (fast+browser) 42/13; `pnpm test:int:slow` (e2e) 3/3;
  ручной прогон через собранный `dist/cli.js` (`init && build --out src/styles/theme.css
  --tailwind src/styles/bridge.css && check`) → `exit 0`, 9 unused-warning, 0 hardcode,
  `no sources scanned` (легитимно — нет консьюмерских исходников); с добавленным
  `src/consumer.css` (`var(--color-nope)`+`#ff0000`) → `error: dead var reference --color-nope`,
  `warning: hardcoded hex #ff0000`, генераты по-прежнему чисты, `exit 1` — дословно совпадает с
  Validation item'а.
  **Known Deviations (material — статус 🟠):** item-коммит несёт дифф вне `Files` item'а:
  `packages/cli/src/commands/build.ts` (default `config` синхронизирован через новый
  `constants.ts` — необходимо, иначе `init && build && check` без `--config` расходится по пути
  темы) и `packages/cli/src/constants.ts`; `tests/integration/src/e2e/cli-init-nuxt.test.ts`
  физически лежит в `src/e2e/` (существующий 3-ярусный vitest-конвейер), а не буквально
  `tests/integration/cli/**` из `Files` — последнего каталога не существует ни в одном
  `vitest.config.ts`-проекте, файл там не подхватился бы ни одним прогоном.
  **Pending Work:** баннер-regex (`GENERATED_BANNER_RE`) не централизован в `@themeon/core`
  (finding предлагала общий экспорт оттуда) — локальная копия в `commands/check.ts`, т.к.
  `packages/core` вне `Files` этого item'а; риск низкий (баннеры стабильны с P1.5/P8.3), не
  заведено отдельным item.
- **Adversarial-review (opus) по коммиту `04aec27`**: 0 HIGH, 2 MED, 4 LOW/nit. MED#1 — path-based
  `scanIgnorePatterns` не проверялась изолированно от content-based баннер-фильтра (все фикстуры
  несли баннер — поломка `--out`/`--tailwind`/`config`-исключений осталась бы незамеченной). MED#2
  — `cli-init-nuxt.test.ts` доказывал только запуск сервера, не то, что dev-watcher реально следит
  за файлом там, куда его положил `init` (не rootDir). Оба устранены коммитом `793b384`: новый
  тест в `scaffold.test.ts` (файл без баннера по фактическому `--out`, 0 hardcode только за счёт
  пути) + `cli-init-nuxt.test.ts` дополнен правкой РЕАЛЬНОГО init-скаффолда после старта `nuxt dev`
  + поллингом перегенерированного CSS без ручного рестарта. 4 LOW/nit — не код-фиксы, зафиксированы
  как знания (glob-спецсимволы в `--out` бэкстопятся баннером, security-регрессии нет; пустой скан
  шумно гоняет coverage; `checkHardcode` поведенческий разрыв не заведён отдельным freeze-тестом,
  осознанно). Перепрогон на `793b384`: build/typecheck/lint зелёные, `pnpm test` 1204/48,
  `pnpm test:int:slow -- cli-init` 3/3.

**Remaining:**

1. **P8.14** — последний item фазы, полностью детализирован в `phases/P8.md`. `/task:plan-exec`
   (sonnet/medium) + ОБЯЗАТЕЛЬНЫЙ adversarial-review (opus/xhigh). После закрытия — **фаза P8
   терминальна**, снимается блокировка P5.9/P5.11/P5.10.
2. **P5.9 / P5.11 / P5.10** (пилоты) — ЗАБЛОКИРОВАНЫ до закрытия ВСЕЙ P8 (решение владельца
   2026-07-14). Разблокируются сразу после P8.14.
3. **P6 / P7** — без изменений.

**Sources of truth:**

- План: `~/projects/packages/themeon/plans/2026.07.12-BASE/` (repo = SSOT; Vault — зеркало).
- Входы фазы P8 — `findings/P8-*.md`, НЕ `20_research/R-xx` (R-11 §1, R-13 §4.3/§4.4, R-14 §2.1 —
  ложные утверждения; P8.14 их размечает как «ОПРОВЕРГНУТО», не удаляет).
- Пакеты: `~/projects/packages/themeon/packages/*` — HEAD (после коммита `793b384`), дерево
  чистое. `dist` БРАТЬ В ПИЛОТЫ НЕЛЬЗЯ до закрытия P8 (P8.14 закрывает).
- `themeon` CLI (`packages/cli`): `checks/contrast.ts` зовёт `checkThemeContrast` из
  `@themeon/colors` (единственный SSOT-канал, дублировать пороги запрещено); `commands/check.ts`
  несёт `scanIgnorePatterns`+`GENERATED_BANNER_RE` (единственное место исключений скана —
  `checks/hardcode.ts`/`checks/coverage.ts` больше НЕ фильтруют по имени файла); `constants.ts`
  несёт `DEFAULT_THEME_CONFIG_PATH = 'theme/theme.config.ts'` — единственный дефолт для
  `init`/`build`/`check`, менять во всех трёх местах разом.
- `@themeon/core` DTCG-мост целиком закрыт (P8.11 эмит + P8.12 импорт) — без изменений с
  прошлого handoff, детали там же.
- Changesets (@themeon/naive+@themeon/core P8.8; naive-деривация P8.9; DTCG-мост P8.11+P8.12) ещё
  не зарелижены (репо не в npm, P-D53) — P8.13 не заводила отдельный changeset (CLI-only фикс,
  та же логика, что P8.11/P8.12).

**Open risks:**

- Тонкий APCA-запас (0.9–1.7 Lc) против `--color-bg-subtle` у пар `focusRing`/`link` в дефолт-теме
  (P8.7 review finding) — сдвиг нейтральной/акцентной шкалы `@themeon/colors` может увести их в FAIL;
  гейт fail-closed поймает на сборке, не сформирован как отдельный item.
- `@themeon/vite`: двойной инстанс `@themeon/core` в графе (наблюдение разведки P8.2, вне скоупа) —
  зафиксирована в Pending Work P8.2, отдельная проверка не заведена как item.
- P8.9 остаточный риск §3.5 (findings): в dark лестница `base→hover→pressed` идёт вверх по L ⇒
  контраст белых чернил падает 75.6→69.0→62.0 — порог `text` (60) держится, `body` (75) нет.
  Неизбежно при Radix-направлении и белых чернилах, не баг реализации.
- P8.10: `$theme` в `ComponentCustomProperties` — глобальная аугментация; потребитель, импортирующий
  `@themeon/vue`, но НЕ ставящий плагин, получит `$theme` типизированным (компилируется), но
  `undefined` в рантайме (неустранимо структурно, тот же паттерн у `$pinia`/`$router`, принят как
  остаточный риск).
- P8.11: имена тем с `.`/`{`/`}` не экранируются для имени файла/resolver-ключа контекста (review
  low finding, не спека-нарушение) — P8.12 читает такие имена файлов через собственную resolver/
  эвристику по содержимому (не парсит basename), риск снят для нормального потока.
- P8.12: `dtcgValueToRaw` для известного, но НЕ DTCG-стандартного `$type` (напр. `"border"`) молча
  пропускает примитивный `$value` без warning (§8.8 запрещает кастомные `$type` вообще) —
  асимметрично с новым поведением для ПОЛНОСТЬЮ отсутствующего `$type` (который теперь warns).
  Пре-P8.12 поведение, не регрессия этого item'а — отдельная находка, не заведена как item.
- P8.13: `GENERATED_BANNER_RE` — локальная копия regex'а в CLI, не централизованный export
  `@themeon/core` (см. Pending Work выше); если текст любого из двух баннеров изменится, regex
  нужно править синхронно вручную.

**Workarounds / Deferred / Open questions:**

- **workarounds:** `spawnNuxtDev` выбирает порт сам и ждёт HTTP-поллингом вместо парсинга stdout
  (P8.1 Known Deviation) — специфика этой среды исполнения, пересмотреть при переносе в CI.
- **deferred:** централизация `GENERATED_BANNER`/`GENERATED_BANNER_RE` в `@themeon/core` (P8.13
  Pending Work, вне Files item'а); `dispose()` у `UseThemeReturn` (P3.8, повторно deferred в
  P8.10 Scope Excluded); перевод русских JSDoc публичных типов на английский (репо-широкий долг,
  P8.14 фиксирует как долг); структурный DTCG-эмит `shadow`/`gradient` (backlog, P7);
  `@themeon/vite`: двойной инстанс core; `experimental.bundledDev` (Vite 8.1.x, открытый
  апстрим-баг); CJS-тем поддержка/документация (P8.4 review finding); e2e granular-vs-restart
  различение (P8.4 review finding); тонкий APCA-запас против `bg-subtle` (P8.7 review finding, см.
  Open risks); «жёлтая полоса» APCA для warning-заливок (P8.8, findings §4.2 — Q владельцу);
  экранирование имён тем с `.`/резолвер-ключей (P8.11 review low finding); `escapeDTCGSegment` не
  разделяет по `_` в отличие от naming-движка (P8.11 review nit); импорт сложных чужих resolver'ов
  с несколькими modifier'ами (P8.12 Scope Excluded, есть warning); `dtcgValueToRaw` не warn'ит на
  известном-но-нестандартном `$type` (P8.12 review finding, см. Open risks).
- **open_questions:** `open-questions.md` — Q3 (`@bg-dev/nuxt-naiveui`, нужен владелец до merge
  веток пилотов), Q4 (генерализация — частично поглощена P8, P8.14 обновляет статус). Q1/Q2/Q5
  закрыты.
