ПЕРЕД ЗАПУСКОМ ЭТОГО ПРОМПТА: dterema-эталон (P5.1–P5.4) закрыт 🟠 Done with deviations. По плану
(`plan.md` §2б/§3, `phases/P5.md` Phase Context) ПЕРЕД началом P5.5/vintera ОБЯЗАТЕЛЕН `/task:review`
dterema-пилота целиком (P5.1–P5.4) — это отдельный плановый гейт, НЕ то же самое, что per-item
adversarial-review (уже пройден 4 раза, по разу на каждый item). Если `/task:review` ещё не
прогонялся — прогони его первым, в отдельной сессии, и только потом переходи к промпту ниже.

---

Роль — исполнение item'а P5.5 фазы P5 плана THEMEON (пилотная миграция dterema/vintera на
ThemeOn), с ОБЯЗАТЕЛЬНЫМ read-only adversarial-review после реализации — прод-риск, ошибка ломает
живой сайт. Работаешь в этой же CLI-сессии, без фоновых Workflow.

Порядок чтения СТРОГО:
1. /home/vostrikov/projects/packages/themeon/plans/2026.07.12-BASE/plan.md — §1 Context, §2
   Execution Rules, §5 Decision Log (особо P-D34..P-D43 — механика P5, parity-семантика, известные
   баги автоскрипта уже починены), §4 Status Board.
2. /home/vostrikov/projects/packages/themeon/plans/2026.07.12-BASE/handoff.md
3. /home/vostrikov/projects/packages/themeon/plans/2026.07.12-BASE/phases/P5.md — Phase Context
   целиком + item P5.5 целиком (Status/Intent/Scope/Implementation Rules/Code Guidance/
   Validation/expectedVarDiff). Прочти также Completion Notes P5.1 (dterema) — метод установки
   ThemeOn (yalc + ручные yarn `resolutions`) и метод снятия Baseline/parity-harness переиспользуются
   для vintera один-в-один; P5.4 Completion Notes/Pending Work — там же package-finding про
   `@themeon/naive` `toNative()` (near-empty `common` для ad-hoc тем, не канон `WellKnownSys`),
   почти наверняка повторится и в vintera (P5.9) — не удивляться заранее.
4. Required Reads item'а, в указанном порядке.

Прод-репо пилотов: dterema `/home/vostrikov/projects/dterema/app`, vintera
`/home/vostrikov/projects/vintera/vintera`. Item P5.5 трогает ТОЛЬКО vintera — ветки
`themeon-migration/P5` там ЕЩЁ НЕТ (создаётся этим item'ом от текущего HEAD, `fix/spa-build-api-url`
на момент последнего аудита 2026-07-13, `676bdcb`, дерево чисто — перепроверь `git status --short`
живьём перед созданием ветки, не доверяй устаревшему факту). ЖЁСТКИЕ инварианты, нарушение =
escalation, не импровизация:
  - INV1: работай ТОЛЬКО в ветке `themeon-migration/P5` vintera. НЕ push, НЕ merge, НЕ rebase на
    основную.
  - INV2: НИКАКИХ `git stash`/`git reset`/`git checkout -f`/`git clean` в прод-репо.
  - INV3 (parity): vintera — ЭТАЛОН РЕГРЕССИЙ (D17), СТРОГИЙ гейт как задумано, БЕЗ послаблений
    dterema (то решение пользователя касалось только dterema, не vintera). Baseline снимается ОДИН
    раз этим item'ом (P5.5) — по методу Code Guidance P5.1 (тот же `themeon-parity.mjs`, тот же
    подход к снятию pristine-снимка ДО любых правок), остальные item'ы vintera (P5.6–P5.10)
    переиспользуют его, не переснимают.
  - Установка ThemeOn — метод «yalc + ОБЯЗАТЕЛЬНЫЕ ручные yarn `resolutions`» (P5.1 Completion
    Notes): yalc НЕ чинит `catalog:`-протокол pnpm и внутренние cross-package `@themeon/*`-зависимости
    вне pnpm-воркспейса — потребуются ручные `resolutions` в `package.json` vintera (5 внутренних
    `@themeon/*` → `file:.yalc/@themeon/*`; catalog-пакеты → версии из `pnpm-workspace.yaml`
    ThemeOn-монорепо, сверяясь с уже установленными в vintera версиями во избежание дублей в дереве).
    `.yalc/` коммитить через `git add -f` (проверить `.gitignore` vintera на anchored/non-anchored
    правила для `dist`, как в dterema P5.1).

Исполняй СТРОГО Scope Included; Implementation Rules и Code Guidance — жёсткие рамки, не
рекомендации. «Улучшить бы заодно» — в Pending Work, не делай (но если идея тянет на генерализацию
пакета для будущих пользователей, а не только этого пилота — вынеси её отдельным абзацем в конце
своего финального сообщения, не молчи о ней; ГЛАВНЫЙ КРИТЕРИЙ ПАКЕТА — гибкость по настройкам,
явное указание пользователя — если приходится обходить пакет хаком в пилоте, это НАХОДКА ПО ПАКЕТУ,
а не норма, зафиксируй явно, не молчи). Комментарии/тест-описания на русском. План разошёлся с
реальным кодом (файла нет, сигнатура другая, факты устарели) — НЕ импровизируй: останавливайся и
опиши расхождение, ничего не коммить.

Прогони Validation-команды item'а, почини красное в рамках Scope. Прогони parity-гейт (после снятия
Baseline этим же item'ом) ДО коммита финального статуса. Закоммить (`git add` конкретных путей, НЕ
`-A`; сообщение на русском, тело — почему; НЕ push) в `~/projects/vintera/vintera`, ветка
`themeon-migration/P5`.

Затем ОБЯЗАТЕЛЬНО подними отдельного read-only adversarial-ревьюера через Agent tool
(subagent_type: general-purpose, model: opus, максимальный effort) на СВОЙ коммит — контекст:
item P5.5, Scope/Code Guidance из P5.md. Фокус ровно на том, что parity по переменным не ловит:
корректность самого Baseline-снимка (снят ДО правок, из pristine донорского вывода, не из
уже-мигрированного состояния), воспроизводимость установки пакетов (resolutions/yalc), отсутствие
случайных правок вне Scope P5.5. Severity LOW/MED/HIGH/CRITICAL, при сомнении — выше. Находки MED+ —
почини сам, отдельным коммитом, перепрогони Validation и parity заново.

В конце: обнови `phases/P5.md` (Status item'а — 🟢 Done / 🟠 Done with deviations + Known
Deviations / 🔴 Blocked с причиной; Completion Notes — только факты; Status Board §4 plan.md).
Перезапиши `handoff.md`. Синхронизируй копию плана в
`/home/vostrikov/Vaults/Brain/05-Projects/03-Packages/ThemeOn/plans/2026.07.12-BASE/` (rsync -a
--delete поверх, только плановые файлы).

Финал ответа — ОБЯЗАТЕЛЬНО: короткий статус (что сделано, вердикт review, что дальше) и ГОТОВЫЙ
К ВСТАВКЕ промпт для следующего item'а — P5.6 (шаблон и таблица подстановок в
`plans/2026.07.12-BASE/workflows/P5-manual-item-prompt.md`). Сохрани его аналогично этому файлу —
`plans/2026.07.12-BASE/workflows/P5.6.prompt.txt` — чтобы следующую сессию можно было запустить той
же командой с заменённым именем файла.
