# OPEN QUESTIONS — THEMEON (2026.07.12-BASE)

Протокол — `plan-protocol` §3. Статусы: `Exploration` · `Decision pending (owner needed)` ·
`Resolved → D#`.

## Q1 — Где чинить `default: ''` (пакет vs пилот)

**Статус:** Resolved → **P-D44** (2026-07-13).
Фикс — в пакете, тремя слоями (`@themeon/vue` нормализует пустое имя темы; `@themeon/nuxt` эмитит
`default: options.default ?? ''` с честным типом). Обход в пилоте запрещён. Обоснование и
RAG — `findings/P3-nuxt-runtimeconfig-default-coercion.md`.

## Q2 — Снимается ли заморозка P3 внутри P5

**Статус:** Resolved → **P-D45** (2026-07-13).
Заморозка не снимается «внутри P5»: фаза P3 переоткрыта отдельным item'ом P3.7 (владелец
артефакта = фаза, его создавшая), пилот ждёт фикс. P5 пакеты по-прежнему не правит.

## Q3 — Судьба `@bg-dev/nuxt-naiveui` в пилотах

**Статус:** Decision pending (owner needed).
После P5.4/P5.9 его color-mode инертен (единственный источник темы — `@themeon/vue`), но модуль
остаётся ради авто-импорта Naive-компонентов; в vintera снят `<NaiveConfig>` (заменён прямым
`<NConfigProvider>`), в `nuxt.config.ts` осиротела опция `naiveui.themeConfig`. Варианты: (a)
оставить как есть (авто-импорт полезен, мёртвая опция безвредна); (b) снять модуль и подключать
Naive-компоненты явно/`unplugin-vue-components`; (c) снять только `naiveui.themeConfig`.
**Нужно от владельца:** решение (a/b/c) до merge веток `themeon-migration/P5` в основные.
Влияние на план: (b) — новый item в P5 или follow-up-фаза; (a)/(c) — строка в Completion Notes.

## Q4 — Генерализация пакета по находкам пилотов

**Статус:** Exploration — **частично поглощена фазой P8** (2026-07-14). `toNative()` без
`varMap`/`roles` оказался не DX-долгом, а Blocker'ом (#2/#3 аудита) и чинится в P8.8/P8.9
(P-D56/P-D57); `--breakpoint-*`-мост — в P8.3 (P-D54). В Exploration остаётся то, что НЕ является
дефектом: опции `aliases`/`refLayer` у `@themeon/nuxt` (P-D34) и переносимость канона на чужие
шкалы. Исторический контекст — ниже.
Пилоты вскрыли класс «пакет жёстко завязан на канон, консьюмеру нечем перенастроить»:
`@themeon/naive` `toNative()` без `varMap`/`roles` (P5.4 Pending Work), `@themeon/nuxt` без опций
`aliases`/`refLayer` (P-D34), breakpoints-мост только JS (P-D41). Это не дефекты (обходы есть и
задокументированы), а DX-долг → кандидаты в отдельную фазу генерализации ПОСЛЕ закрытия P5, не
патчи по ходу пилотов.

## Q5 — `plans/` не под git в репозитории пакета

**Статус:** Resolved (2026-07-14, коммит `45da5d3`) — `plans/` закоммичена в репозиторий пакета,
repo остаётся SSOT на время exec, Vault — зеркало. Коммит-гейт протокола снова работает.
Исторический контекст вопроса — ниже.
`git status` в `~/projects/packages/themeon`: `?? plans/` — директория плана не отслеживается, хотя
`plan.md` §2/Meta объявляют repo источником истины на время exec (Vault — зеркало). Коммит-гейт
протокола (§6: «bookkeeping-дифф закоммичен») на неотслеживаемом плане не работает: история решений
живёт только в рабочем дереве и в Vault-копии.
**Нужно от владельца:** закоммитить `plans/` в репо (и убрать из `.gitignore`, если он её прячет)
ИЛИ явно зафиксировать, что SSOT плана — Vault, а repo-копия рабочая и не версионируется.
