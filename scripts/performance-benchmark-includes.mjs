/** SSOT scenario keys gated against `performance-baseline.json` (P2.5). */
export const performanceBenchmarkIncludes = Object.freeze([
  'small.compileWarmMs',
  'medium.compileWarmMs',
  'large.compileWarmMs',
  'wideComponent.compileWarmMs',
  'tenantPatch.compileWarmMs',
  'small.applyRuntimeMs',
  'small.dtcgRoundTripMs',
  'vitePlugin.loadWarmMs',
])
