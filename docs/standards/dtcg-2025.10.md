# DTCG Format Module 2025.10 — ThemeOn support matrix (P0.1)

**Primary source (RAG:✅):** [Design Tokens Format Module 2025.10](https://www.designtokens.org/TR/2025.10/format/) — retrieved **2026-09-19**.

**Policy (D2):** DTCG is interchange, not authoring. Unknown metadata/extensions are preserved opaquely when safe; otherwise loss is **reject-with-diagnostic** by default (`allowLossy: false` on `fromDTCG`) or **warn** when explicitly allowed.

Legend: **Import** / **Export** / **Validate** / **Lossless** refer to current `packages/core/src/dtcg/*` behavior (v1, P-D15). Gaps name the owning plan item.

| Feature | Import | Preserve in IR | Validate | Export | Loss handling | Fixture / test |
|:--|:--|:--|:--|:--|:--|:--|
| `$value` (primitive) | ✅ structural + legacy string | value only | type required (inherit or token) | structural when possible | reject/skip with `THEMEON_DTCG_UNRECOGNIZED_VALUE` | `fixtures/color-string.json`, `from-dtcg.test.ts` |
| `$type` on token | ✅ | via token type | ✅ | ✅ | unknown → `THEMEON_DTCG_UNKNOWN_TYPE` | `from-dtcg.test.ts` |
| `$type` group inheritance (incl. root) | ✅ | ✅ | ✅ | ✅ | missing type → skip + diagnostic | `fixtures/group-type-inherit.json` |
| `$description` | ⚠️ ignored | ❌ P1 gap | — | not emitted | warn (future P1) | P1 gap |
| `$deprecated` | ⚠️ ignored | ❌ P1 gap | — | not emitted | P1 gap | P1 gap |
| `$extends` | ❌ v1 | ❌ | — | ❌ | `THEMEON_DTCG_UNSUPPORTED_EXTENDS` | `fixtures/root-extends.json` |
| `$extensions` (token) | ⚠️ | ❌ P1 | — | bridge on export only | `THEMEON_DTCG_EXTENSIONS_NOT_CARRIED` / `DTCG_LOSSY_IMPORT` | `fixtures/extensions-token.json` |
| Curly-brace alias | ✅ | Token ref | ✅ | ✅ curly | unresolved → `THEMEON_DTCG_UNRESOLVED_ALIAS` | `fixtures/alias-curly.json` |
| `$ref` → `#/…/$value` | ✅ import-only | Token ref | partial | not emitted | property-level → `THEMEON_DTCG_UNSUPPORTED_REF` | `from-dtcg.test.ts` |
| `$root` | ❌ v1 | ❌ | — | ❌ | `THEMEON_DTCG_UNSUPPORTED_ROOT` | `from-dtcg.test.ts` |
| Color structural 2025.10 | ✅ | color string | ✅ | ✅ | unrepresentable → bridge | `fixtures/color-structural.json` |
| Dimension `{value,unit}` | ✅ px/rem export | string | unit gate | px/rem only | other units → bridge + `THEMEON_DTCG_EXPORT_UNREPRESENTABLE` | `fixtures/dimension-structural.json` |
| Composite shadow/gradient | string only | partial | — | bridge only | `THEMEON_DTCG_COMPOSITE_UNSUPPORTED` | P2 fixtures |
| Typography composite | partial → text | TextStyleValue subset | — | primitives not typography | `THEMEON_DTCG_EXPORT_TEXT_DEGRADED` | round-trip test (P8.12) |
| Token + group mix (§6.1) | ❌ | — | ✅ throw | — | `DTCG_PARSE` | `fixtures/token-group-mix.json` |
| Multi-file / resolver | heuristic + resolver doc | ✅ | partial | `themeon.resolver.json` | bundle diagnostics | `from-dtcg.test.ts` |

## P1 / P2 gap list (explicit)

1. **P1:** Carry `$description`, `$deprecated`, token/group `$extensions`, and `com.themeon` bridge maps in IR (lossless round-trip for text/shadow/gradient).
2. **P1:** `$extends` / `$root` group semantics.
3. **P2:** Property-level `$ref`, full composite structural forms, resolver beyond sets + one modifier.

## Machine-readable manifest

Row coverage is enforced by `packages/core/src/dtcg/matrix-coverage.test.ts` against `packages/core/src/dtcg/fixtures/matrix-manifest.json`.
